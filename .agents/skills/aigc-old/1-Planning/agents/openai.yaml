interface:
  display_name: "AIGC 规划阶段"
  short_description: "单一 Skill 2.0 包统筹分集、格式、分组与阶段验收"
  default_prompt: "Use $aigc-planning as the single fused Skill 2.0 package for the AIGC planning stage: route the request into episode_split, script_format (display name: 2-剧本), grouping, full_chain, stage_validation, or repair mode; load only the matching references, scripts, templates, types, review, and knowledge-base files; preserve the project runtime outputs under 1-分集/2-格式/3-分组; aggregate only executed results into validation-report.md; and hand off to 2-Global."
