interface:
  display_name: "AIGC 分镜故事板参照"
  short_description: "融合全能参照、Assets 绑定与视频生成 handoff"
  default_prompt: "Use $aigc-video-storyboard-reference when a 6-Video task should create one fused storyboard reference package: read `projects/aigc/<项目名>/3-Detail/第N集.json`, LLM-author group-level omni-reference prompt/TXT artifacts, bind real storyboard Assets references when needed, and prepare provider `submit-plan.json` plus `submit-brief.md` without deleting the legacy `全能参照`, `2-参照引用`, or `3-视频生成` skill packages."
