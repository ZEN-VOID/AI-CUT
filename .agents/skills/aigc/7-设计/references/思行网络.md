# 7-设计 思行网络

## Node Network

| node_id | input | action | output | gate |
| --- | --- | --- | --- | --- |
| `D-N1-INTAKE` | 用户请求、项目名、集数、候选输入文件 | 锁定项目根与 7-设计 输出根 | `design_scope` | `projects/aigc/<项目名>/7-设计/` 可定位或可创建 |
| `D-N2-DOMAIN` | `design_scope` | 判定命中 `场景 / 角色 / 道具` 域集合 | `domain_routes` | 至少一个 active 域 |
| `D-N3-RECONCILE` | `domain_routes`、上游 `6-分组`、既有 7-设计产物 | 识别全量/增量范围、已消费集数、主体合并与缺口 | `reconcile_delta` | 分批上游不得跳过对账 |
| `D-N4-DISPATCH` | `domain_routes`、`reconcile_delta` | 加载对应域级 `SKILL.md + CONTEXT.md` | `domain_execution_plan` | 不调度未命中域，只处理缺口 |
| `D-N5-DOMAIN-GATE` | 域级输出 | 检查域内 `1-清单 + 2-设计 + 3-生成` leaf 产物 | `domain_verdicts` | 输出位于对应域内子目录且未静默覆盖 |
| `D-N5R-DOMAIN-REPAIR` | `domain_verdicts` 中的失败 findings | 路由回对应域级 leaf 执行 direct repair 并复审 | `domain_repair_results` | 父级不直接补写业务真源；失败域复审通过或明确阻断 |
| `D-N6-CLOSEOUT` | `domain_verdicts`、`domain_repair_results` | 按需写阶段 `validation-report.md` | `stage_verdict` | 失败可回指具体域级 owner；未通过域不得伪装通过 |

## Failure Routing

| symptom | route |
| --- | --- |
| 旧 tranche 路径仍被引用 | `D-N2-DOMAIN -> registry/routes/shared runtime 修复` |
| 新增 `6-分组` 后重复主体、漏设计或静默覆盖 | `D-N3-RECONCILE -> incremental-reconciliation-contract.md -> 对应域 1-清单/2-设计/3-生成` |
| 域级包缺 Skill 2.0 分区 | `D-N4-DISPATCH -> skill-工作车间结构修复` |
| 父级或下游仍要求 7-设计 根目录平铺业务真源 | `D-N5-DOMAIN-GATE -> 对应域 SKILL.md Output Contract` |
| 设计模板或面板 JSON 漂移 | `D-N5-DOMAIN-GATE -> 对应域 templates/review` |
| 域级 review 失败后父级试图直接补业务正文 | `D-N5R-DOMAIN-REPAIR -> 对应域 leaf review/review-contract.md` |

## Review Gate Mapping

| Review Question | Review Gate | Fail Code | Rework Target | Report Evidence |
| --- | --- | --- | --- | --- |
| 是否在进入域级调度前锁定了项目根、7-设计输出根、集数、候选输入文件与可创建/可定位状态？ | `GATE-DESIGN-ROUTE-01` | `FAIL-DESIGN-ROUTE-SCOPE` | `D-N1-INTAKE`；`references/思行网络.md` | `design_scope`、project root、episode/input roots、路径可定位或可创建说明 |
| `domain_routes` 是否只包含 active 的 `场景 / 角色 / 道具`，且未把旧 tranche 或 pending sibling 当作域级入口？ | `GATE-DESIGN-ROUTE-02` | `FAIL-DESIGN-ROUTE-DOMAIN` | `D-N2-DOMAIN`；`references/阶段路由矩阵.md` | `domain_routes`、active/pending 判定、旧路径改路由记录 |
| 分批上游、已存在 7-设计产物、重复主体、漏设计或覆盖风险是否先进入 `D-N3-RECONCILE`，而不是直接 dispatch？ | `GATE-DESIGN-INC-01` | `FAIL-DESIGN-INC-SOURCE-SCOPE` | `D-N3-RECONCILE`；`references/incremental-reconciliation-contract.md` | `source_scope`、`previous_scope`、已读取输入和既有产物摘要 |
| `reconcile_delta` 是否完整记录新增、归并、重命名、失效来源、设计缺口、生成缺口和覆盖风险？ | `GATE-DESIGN-INC-03` | `FAIL-DESIGN-INC-DELTA-INCOMPLETE` | `D-N3-RECONCILE` | `reconcile_delta` 完整字段记录 |
| `D-N4-DISPATCH` 是否只加载命中域的 `SKILL.md + CONTEXT.md`，只处理缺口，并避免补跑未命中域？ | `GATE-DESIGN-ROUTE-03` | `FAIL-DESIGN-ROUTE-DISPATCH` | `D-N4-DISPATCH`；对应域级 `SKILL.md + CONTEXT.md` | `domain_execution_plan`、已加载合同、处理/跳过范围 |
| `D-N5-DOMAIN-GATE` 是否检查域内 `1-清单 + 2-设计 + 3-生成` 产物都落在对应域内子目录，且未静默覆盖？ | `GATE-DESIGN-CLOSEOUT-01` | `FAIL-DESIGN-CLOSEOUT-DOMAIN-GATE` | `D-N5-DOMAIN-GATE`；对应域级 Output Contract | `domain_verdicts`、canonical output paths、覆盖/跳过记录 |
| 域级 review 失败时，父级是否只路由到 leaf direct repair + re-review，而没有直接补写清单、设计或生成正文？ | `GATE-DESIGN-CLOSEOUT-02` | `FAIL-DESIGN-CLOSEOUT-REPAIR-ROUTE` | `D-N5R-DOMAIN-REPAIR`；对应域级 leaf `review/review-contract.md` | failed findings、repair owner、repair action、re-review verdict |
| `D-N6-CLOSEOUT` 是否只在失败域可回指 owner、通过域复审完成或明确阻断后写阶段 verdict，且未把未通过域伪装为 pass？ | `GATE-DESIGN-CLOSEOUT-03` | `FAIL-DESIGN-CLOSEOUT-REPORT` | `D-N6-CLOSEOUT`；`Stage-Closeout Review-Repair Contract` | `validation-report.md`、domain verdict matrix、residual risks |

## Mermaid

```mermaid
flowchart TD
    A["D-N1 Intake"] --> B["D-N2 Domain Route"]
    B --> R["D-N3 Incremental Reconcile"]
    R --> C1["场景 Skill 2.0"]
    R --> C2["角色 Skill 2.0"]
    R --> C3["道具 Skill 2.0"]
    C1 --> D["D-N5 Domain Gate"]
    C2 --> D
    C3 --> D
    D -->|"needs_rework"| X["D-N5R Domain Repair"]
    X --> D
    D -->|"pass"| E["D-N6 Stage Closeout"]
```
