version: 1
interface:
  display_name: "AIGC 5-设计"
  short_description: "路由到场景、角色、道具三个融合后的 Skill 2.0 设计包。"
  default_prompt: "Use $aigc-design to route the current 5-设计 task into exactly the needed fused domain package: `$aigc-design-scene`, `$aigc-design-role`, or `$aigc-design-prop`. Each domain package runs list -> design -> panel prompt in order and writes `[域]清单.md`, `[主体名].md`, and `[主体名].json` directly under `projects/aigc/<项目名>/4-设计/`."
