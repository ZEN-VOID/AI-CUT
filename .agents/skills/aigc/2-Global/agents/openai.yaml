interface:
  display_name: "AIGC 2-Global"
  short_description: "用单技能并发链先定稿三份 Markdown，再把按字段确认的结果与完整组正文提取进 shared episode root"
  default_prompt: "Use $aigc-global to read the current episode grouping plus init presets, run the internal global-style, type-guidance, and director-intent capability chains with a convergence gate, confirm the Markdown fields first, then write or refresh 全局风格.md、类型元素.md、导演意图.md and extract their titled fields into the shared episode root."
