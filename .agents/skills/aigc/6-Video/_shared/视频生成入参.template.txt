提示词
<填写最终提示词正文：默认以连贯自然语句串联分镜内容，尽量覆盖全部字段；每个分镜保留当前分镜组内的 xx秒-xx秒，且 `分镜ID` 与时间之间不得出现“的”；镜级信息可优先按“镜头属性 -> 景别/运镜手法/镜头速度/镜头视角 -> 角色站位走位/角色背景面 -> 角色表现/场景氛围 -> 道具及状态/摄影美学/其他”的语义顺序组织，但不强制固定句式，自然流畅和信息覆盖优先；`镜头属性` 指定场镜头、反应镜头等专有功能名词，优先沿用上游原词，并直接落术语本体，不要额外加“为”；保留分镜组ID/分镜ID显式标签；除分镜组ID/分镜ID外不显式写字段标题，不得写成 `字段标题：字段值`；目标字数以 prompt_style.char_limit 为准；当且仅当字数吃紧时，才允许局部短语化；超限时先压缩低优先级补充项，再压缩重要表现项，高优先保留 xx秒-xx秒、角色站位走位/角色背景面/景别/运镜手法/镜头速度（如存在）/镜头视角>

字数统计:
<prompt_char_count>
