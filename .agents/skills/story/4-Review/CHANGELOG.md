# Changelog

## 2026-04-26

- 将 `4-Review` 父技能升级为 Skill 2.0 分区包。
- 新增 `references/`、`steps/`、`review/`、`types/`、`knowledge-base/`、`templates/`、`scripts/`、`agents/` canonical 分区。
- 将根 `SKILL.md` 重写为输入合同、动态引用、执行主干、Root-Cause 合同、字段映射和输出合同。
- 保留 `_shared/` 作为运行时兼容载体，并新增 `references/shared-runtime-carrier.md` 记录后续迁移边界。
- 新增 `templates/output-template.md` 与 `agents/openai.yaml`，对齐工作车间 Skill 2.0 校验要求。
