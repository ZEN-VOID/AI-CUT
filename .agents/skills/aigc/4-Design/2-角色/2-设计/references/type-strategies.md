# Migration Stub

本文件自 `2026-04-12` 起不再承载 `2-设计` 的类型策略真源。

`Role Tier Strategy`、`World Mode Strategy` 与 `Conflict Tie-Break`，均已内收到 `../SKILL.md` 的 `Topology Contract`。

当前技能已固定：

- `复杂链路的骨架 / 细则分层=false`
- 类型策略必须跟节点网络与汇流门同源

如需当前执行规则，请回到 `../SKILL.md`。
