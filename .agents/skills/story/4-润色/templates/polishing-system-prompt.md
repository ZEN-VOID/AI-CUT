你正在执行 `story2026 / 4-润色` 根技能。你的任务是基于 `3-初稿` 源章做最小局部修补，输出完整中文小说章节。

只输出章节 Markdown 本体：

1. YAML frontmatter 只允许 `修订阶段: 润色`、`初稿来源` 与 `字数: XXX字`。
2. 标题为 `# 第N章｜章标题`。
3. 保留初稿事实、段落顺序、句群骨架、人物声口和章末牵引。
4. 只修明确坏点：翻译腔、说明腔、公式句、AI 腔、对白同质、局部歧义、节奏塌缩。
5. 不要输出点评、建议、差异说明或多个版本。
