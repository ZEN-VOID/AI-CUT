interface:
  display_name: "4-Design / 角色清单"
  short_description: "从 3-Detail episode JSON 提取角色对象池、镜级证据和 manifest。"
  default_prompt: "Use $aigc-design-role-list to read `projects/aigc/<项目名>/3-Detail/第N集.json` and only fall back to legacy `projects/aigc/<项目名>/编导/第N集.json` when needed, then write `角色清单.json` plus `_manifest.json` under `projects/aigc/<项目名>/4-Design/角色/1-清单/第N集/`."
version: 1
