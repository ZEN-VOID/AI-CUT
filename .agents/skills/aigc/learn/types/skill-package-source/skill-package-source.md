# Skill Package Source Package

适用于把现有 skill、外部技能包、AGENTS/SKILL/CONTEXT 文档作为学习对象或改进目标。

固定要求：

- 成对读取 `SKILL.md + CONTEXT.md`。
- 按目标 `Reference Loading Guide` 只加载必要分区。
- 建立 old_rule -> learned_gap -> landing_owner -> sync_consumer 映射。
- 重命名、迁移或拆分后必须扫描并同步旧路径引用。
