# Group Visual Tone Contract

本文件定义每个分镜组的组级画面属性输出标准。画面属性从上游 4-摄影 的镜头设计中提炼，以自然语句写入每个分镜组。

## Core Rule

每个分镜组在组装时，必须从组内镜头设计中提炼组级画面属性，写成自然语句。画面属性覆盖该组的构图布局核心选择、构图方式关键子维度、光源效果、色彩基调和关键摄影技术参数。

画面属性不是 north_star.yaml 的风格投影——两者是不同层面：
- **north_star 风格行**（项目级）：全局风格提示词、类型元素提示词、画面风格——由 `north-star-projection-contract.md` 管理
- **画面属性**（组级）：从组内镜头设计提炼的视觉特征——由本合同管理

## Fields

### 构图布局 composition_layout

从组内镜头设计中提炼：主体位置与占比倾向、陪体/前景/背景的主要空间安排。

### 构图方式 composition_method

从 `scene-visual-constraint-contract.md` 的七个子维度中，选取当前组最关键的 2-3 个：
- 形状感：画面主导形状及其心理暗示
- 线条感：画面主导线条方向及视线引导
- 影调感：明暗对比倾向
- 虚实感：景深焦点倾向
- 节奏感：元素重复/渐变/突变倾向
- 纹理质感：画面质地效果
- 气势：画面整体情绪感染力

### 光源效果 lighting

主光源方向、质感、强度的可感知结果；辅助光和逆光如有重要作用也纳入。

### 色彩基调 color

色相主导、色温倾向、饱和度策略的可感知结果。

### 关键摄影技术参数 camera_params

影响画面质感的关键选择（焦距、光圈等），只写对该组画面影响最大的 1-2 项。

### 观看位置与空间感 viewer_position

从组内镜头设计中提炼：本组是否主要使用正面平视、低机位、贴地前景、遮挡视角、手持微晃、浅景深前景或逐步揭示。若组内包含人物行走、入场、压迫、群像或空间建立，画面属性应说明观众如何被带入空间，而不是只写“画面清晰”或“正面展示”。

## Output Rules

1. 画面属性以自然中文语句呈现，覆盖上述五个分区中对该组画面最关键的维度。不要求五个分区全部覆盖，选取最重要的即可。
2. 不输出参数清单、标签列表或模板填空。
3. 画面属性出现在每个分镜组的 `入场镜头：` 之后、north_star 风格行之前。
4. 同一场景内镜头设计视觉特征不变时，相邻分镜组可使用相同或相似的画面属性语句；视觉特征发生变化时（如场景内从平静到恐慌），画面属性必须更新。
5. 画面属性从组内镜头设计中**提炼**，不是从 4-摄影 的内部 `scene_visual_constraint` 直接搬运。提炼时应综合组内各分镜的构图、光源、色彩、摄影参数、机位高度、前景层次和观众发现路径，形成统一的视觉描述。
6. 若组内镜头主要是正面平视全信息展示，必须判断这是有意的中立观察/制度压迫/固定框线，还是扁平摆拍风险；后者应回到 `4-摄影` 修复，不在 `5-分组` 用画面属性补救。

## Knowledge Base Binding

画面属性的裁决和落盘以 `knowledge-base/摄影构图/`（路径：`.agents/skills/aigc/4-摄影/knowledge-base/摄影构图/`）作为知识库参考。裁决时应参考其中的构图技法和影像创造理论，确保画面属性语句在构图布局、构图方式、光源、色彩等维度上有摄影构图理论支撑，而不是凭直觉生成。

## Output Example

```markdown
## 1-1-1

场景1：内景 教室 - 日

入场镜头：教室前排课桌区内，林寂坐在第二排靠窗座位，身体朝向讲台但脸偏向右侧的苏红叶；苏红叶站在林寂课桌右前方半步处，面向林寂。

画面属性：课桌行列形成水平线重复节奏，对称构图把空间压成矩形牢笼；冷白日光灯从正上方打硬光，高反差让白色校服过曝、黑窗帘完全吞光；50mm 标准焦段保持观察距离。

画面属性：低机位贴近地面，前景脚步和衣摆先遮住画面下半部，人物朝镜头走来时透视被拉长；轻微手持和浅景深前景让观众先感到现场距离，再逐步看清主体。

视频生成的画面风格... <全局风格>
<类型元素>
<细分风格>

环境描写：冷白日光灯压在课桌表面...
```

## Review Gate Mapping

| Review Question | Review Gate | Fail Code | Rework Target | Report Evidence |
| --- | --- | --- | --- | --- |
| 每个分镜组是否在入场镜头之后、north_star 风格行之前输出 `画面属性：` 自然语句？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Core Rule`、`Output Rules` | 执行报告记录每组画面属性位置、缺失项和修复后的组头顺序。 |
| 画面属性是否从组内 `4-摄影` 镜头设计提炼，而不是从 north_star、内部约束或模板词直接搬运？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Core Rule`、`Output Rules` | 执行报告记录画面属性对应的组内镜头设计证据、提炼依据和搬运风险检查。 |
| 画面属性是否覆盖构图布局核心选择、构图方式 2-3 个关键子维度、光源效果、色彩基调和最关键的 1-2 项摄影技术参数？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Fields` | 执行报告记录每组画面属性覆盖的构图/光源/色彩/技术参数维度和缺项修复。 |
| 画面属性是否说明必要的观看位置与空间感来源，例如机位高度、前景层次、遮挡视角、透视关系、手持微晃或观众发现路径？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `viewer_position` | 执行报告记录观看位置/空间感摘录、对应上游镜头和遗漏修复结果。 |
| 画面属性是否是自然中文语句，而不是参数清单、标签列表、模板填空或 `scene_visual_constraint` 复制段？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Output Rules` | 执行报告记录清单化/模板化检查、被重写的画面属性文本和自然化结果。 |
| 同一场景相邻组视觉特征不变时是否允许相似语句，视觉特征变化时是否更新画面属性，而不是全场复用同一句？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Output Rules` | 执行报告记录相邻组视觉特征比对、复用理由或更新依据。 |
| 组内镜头若主要是正面平视全信息展示，是否判断其为有意中立观察/制度压迫/固定框线，或回退 `4-摄影` 修复扁平摆拍风险，而不是靠画面属性美化？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、必要时回退 `4-摄影`、本文件 `Output Rules` | 执行报告记录平视展示组 ID、意图判断、扁平风险结论和回退/修复说明。 |
| `knowledge-base/摄影构图/` 是否只作为裁决参考，没有替代组内镜头设计证据或生成脱离当前组的审美语句？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Knowledge Base Binding` | 执行报告记录引用的构图知识维度、当前组证据回指和知识库越界检查。 |
| `Output Example` 是否仅作为形态示例，没有把示例中的教室、日光灯、焦段或构图固定套入无关分镜组？ | `GATE-GROUP-12` | `FAIL-GROUP-10` | `N4-VISUAL-TONE`、本文件 `Output Example` | 执行报告说明示例未模板化；若出现示例污染，记录被替换的硬编码视觉元素。 |
