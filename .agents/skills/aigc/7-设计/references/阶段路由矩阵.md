# 7-设计 阶段路由矩阵

## Active Domain Packages

| trigger | route | load order | final outputs |
| --- | --- | --- | --- |
| 场景、scene、场景清单、场景设计、场景生成 | `.agents/skills/aigc/7-设计/场景` | `场景/SKILL.md + 场景/CONTEXT.md` | `场景/1-清单/场景清单.md`、`场景/2-设计/S###-<场景名>.md`、`场景/3-生成/<场景名>-*.json` |
| 角色、role、character、角色清单、角色设计、角色生成 | `.agents/skills/aigc/7-设计/角色` | `角色/SKILL.md + 角色/CONTEXT.md` | `角色/1-清单/角色清单.md`、`角色/2-设计/<角色名>.md`、`角色/3-生成/<角色名>-*.json` |
| 道具、prop、道具清单、道具设计、道具生成 | `.agents/skills/aigc/7-设计/道具` | `道具/SKILL.md + 道具/CONTEXT.md` | `道具/1-清单/道具清单.md`、`道具/2-设计/<道具名>.md`、`道具/3-生成/<道具名>-*.json` |

## Deprecated Tranche Paths

以下路径仅作为迁移历史，不再作为 active skill 入口：

- `.agents/skills/aigc/7-设计/1-清单`
- `.agents/skills/aigc/7-设计/2-设计`
- `.agents/skills/aigc/7-设计/3-面板`

旧合同已迁入：

- `.agents/skills/aigc/7-设计/references/legacy/`
- `.agents/skills/aigc/7-设计/{场景,角色,道具}/references/legacy/`

## Runtime Rule

所有域级业务真源统一写入对应域内 leaf 子目录：

```text
projects/aigc/<项目名>/7-设计/{场景,角色,道具}/{1-清单,2-设计,3-生成}/
```

域级包可以保留旧 JSON 作为兼容侧车，但最终验收只认：

```text
<域>/1-清单/<域>清单.md
<域>/2-设计/<主体名>.md
<域>/3-生成/<主体名>-*.json
```

## Incremental Runtime Rule

当 `6-分组` 分批完成或后续追加 `第N集.md` 时，`7-设计` 必须先执行增量对账，不得把既有产物视为可静默覆盖的一次性全量输出。统一规则见：

```text
.agents/skills/aigc/7-设计/references/incremental-reconciliation-contract.md
```

每个域可以维护 sidecar 状态索引：

```text
projects/aigc/<项目名>/7-设计/{场景,角色,道具}/design-manifest.yaml
```

`design-manifest.yaml` 只记录已消费上游、主体映射、设计稿路径、生成资产路径和缺口；清单主体真源仍以 `<域>/1-清单/<域>清单.md` 为准。

## Review Gate Mapping

| Review Question | Review Gate | Fail Code | Rework Target | Report Evidence |
| --- | --- | --- | --- | --- |
| 触发词是否只路由到当前 active 的 `场景 / 角色 / 道具` 域级包，且未把旧 tranche 或 pending sibling 当作 active route？ | `GATE-DESIGN-ROUTE-02` | `FAIL-DESIGN-ROUTE-DOMAIN` | `D-N2-DOMAIN`；`references/阶段路由矩阵.md` | 触发词、route、active/pending 判定、被拒绝的旧入口 |
| 每个命中域是否按 `SKILL.md + CONTEXT.md` 加载，而不是只读取 reference、只跑脚本或跳过域级经验层？ | `GATE-DESIGN-ROUTE-03` | `FAIL-DESIGN-ROUTE-DISPATCH` | `D-N4-DISPATCH`；对应域级 `SKILL.md + CONTEXT.md` | load order、已加载合同、处理范围 |
| 最终验收是否只认对应域内 `1-清单 / 2-设计 / 3-生成` canonical 输出，而不是 `7-设计` 根目录平铺文件、旧 JSON 或 sidecar？ | `GATE-DESIGN-CLOSEOUT-01` | `FAIL-DESIGN-CLOSEOUT-DOMAIN-GATE` | `D-N5-DOMAIN-GATE`；对应域级 Output Contract | canonical output paths、根目录/旧 JSON 检查、manifest sidecar 边界 |
| Deprecated tranche paths 是否仅作为迁移历史保留，且不再被 registry、routes、脚本或用户入口当作 active skill？ | `GATE-DESIGN-LEGACY-01` | `FAIL-DESIGN-LEGACY-ACTIVE-ENTRY` | `D-N2-DOMAIN`；`references/阶段路由矩阵.md` | 旧路径引用、改路由结果、legacy archive 声明 |
| 旧 `1-清单 / 2-设计 / 3-面板` 合同中的规则是否未经 active 域级合同复核就被直接执行？ | `GATE-DESIGN-LEGACY-02` | `FAIL-DESIGN-LEGACY-UNVALIDATED-RULE` | `D-N4-DISPATCH`；对应 active 域级 `SKILL.md + CONTEXT.md` | 被复用 legacy rule、active 合同位置、采用/废弃理由 |
| 旧路径、旧 catalog、旧 manifest 口径是否仍冒充 canonical truth，或引用更新后仍有无法自动处理的遗留项？ | `GATE-DESIGN-LEGACY-03` | `FAIL-DESIGN-LEGACY-PATH-DRIFT` | `D-N2-DOMAIN -> registry/routes/shared runtime 修复`；`references/阶段路由矩阵.md` | `rg` 搜索结果、更新引用清单、无法自动更新的遗留引用 |
| 当 `6-分组` 分批完成或追加集数时，是否先走增量对账并锁定 source scope / previous scope，而不是静默覆盖既有产物？ | `GATE-DESIGN-INC-01` | `FAIL-DESIGN-INC-SOURCE-SCOPE` | `D-N3-RECONCILE`；`references/incremental-reconciliation-contract.md` | `source_scope`、`previous_scope`、已读取输入和既有产物摘要 |
| `design-manifest.yaml` 是否仅作为 sidecar 状态索引，没有替代 `<域>/1-清单/<域>清单.md`、设计稿或生成资产真源？ | `GATE-DESIGN-INC-02` | `FAIL-DESIGN-INC-MANIFEST-TRUTH` | `D-N3-RECONCILE`；`references/incremental-reconciliation-contract.md` | manifest 与 canonical 文件路径对照、冲突处理说明 |
