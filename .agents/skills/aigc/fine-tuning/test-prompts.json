[
  {
    "id": "stage-output-tuning-screenplay",
    "prompt": "Use $fine-tuning to improve projects/aigc/Demo/4-编剧/第3集.md for stronger audiovisual action and hook, without changing source facts.",
    "expected": "Build a target stage map for 4-编剧, select the screenplay scheme, run two LLM-first tuning rounds, compare baseline and candidate, and output an owner handoff patch."
  },
  {
    "id": "research-augmented-image",
    "prompt": "Use $fine-tuning on a 9-图像 storyboard prompt and apply high-quality film visual references plus provider constraints before acceptance.",
    "expected": "Load the image-stage scheme, create a reference brief with boundaries, tune prompt atoms through multiple rounds, verify provider result shape, and produce a comparison acceptance matrix."
  },
  {
    "id": "review-only-grouping",
    "prompt": "Use $fine-tuning to review whether an 8-分组 output is ready for 9-图像 and 10-画布, but do not rewrite it.",
    "expected": "Use review_only route, map the grouping scheme, compare baseline against grouping acceptance gates, check owner boundaries, and return pass or needs_rework."
  },
  {
    "id": "cross-stage-alignment",
    "prompt": "Use $fine-tuning to align 2-美学 visual tone, 6-分镜 composition, and 7-摄影 movement for the same episode.",
    "expected": "Create per-stage scheme rows, build source/reference evidence, tune or review each target separately, then compare cross-stage continuity and hand off patches to the correct owners."
  }
]
