# CHANGELOG

## 2026-05-29

- 初始化 BYKJ `05-资产提取` 父级 router。
- 固定默认输入为 `output/[项目名]/02-剧本处理/`。
- 声明角色、场景、道具三个子技能路由与 canonical 输出目录。
- 调整阶段语义为“主体信息提取 -> 设计规格生成 -> JSON 交付”，父级汇总输出改为 `asset-index.json`、`资产提取报告.json` 和 `manifest.json`。
