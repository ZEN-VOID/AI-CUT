# TODO

- [ ] 未来若旧 `分镜故事板` prompt 组装 spec 发生变化，同步复核 `references/request-distillation.md` 与 `steps/storyboard-sheet-workflow.md` 的完整版蒸馏方法。
- [ ] 若 `2-参照引用` 增加新的 provider-specific 引用模式，同步更新 `references/provider-modules.md`。
- [ ] 若 `3-图像生成` 默认 provider 或输出路径合同改变，同步更新 `references/generation-handoff.md` 与 `templates/output-template.md`。
