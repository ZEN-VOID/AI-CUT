# Type Map

## 类型包加载边界

- 每次调用本技能时，必须依据本文件识别并加载同目录 `types/` 中选中的类型包（单选或多选）。
- `types/` 中命中的类型包作为固定上下文加载；`knowledge-base/` 只作为按需检索、切片或向量召回的知识库，不替代类型包。


| signal | route |
| --- | --- |
| 开篇承接断带 | `1-单章叙事起盘` |
| 场景转场突兀 | `2-节奏优化` |
| 活跃 thread 被放掉 | 最早受影响 drafting step |
