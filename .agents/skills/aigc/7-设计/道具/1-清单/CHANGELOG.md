# CHANGELOG

## 2026-04-25

- 补齐 `SKILL.md` 的 `Visual Maps`，增加主流程、模式状态机和类型路由 Mermaid 拓扑，响应 Skill 2.0 工作车间的动态引用可视化要求。
- 补齐 `steps/prop-list-workflow.md` 的工作流 Mermaid 与时序图，使节点、失败回路、review gate 和 canonical 落盘路径可视化。
- 初始化 `aigc-prop-list` Skill 2.0 包结构。
- 建立上游 `6-分组` 组底 YAML `道具` 字段到 `7-设计/道具/1-清单/道具清单.md` 的合同。
- 固定道具清单三列字段、别名归并、背景杂物过滤和脚本只做机械辅助的边界。
