interface:
  display_name: "workflow"
  short_description: "HyperFrames-native reference video workflow"
  default_prompt: "Use $workflow to build a HyperFrames-native reference-rhythm video for short-video platform C-end viewers from script, voiceover or transcript, source media, optional reference clips, and a target output directory."
