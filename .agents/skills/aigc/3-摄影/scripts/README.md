# scripts

本目录只承载 `3-摄影` 的机械辅助脚本，不承担镜头语言、节拍判断、构图、运镜、转场、光影或色彩的主创工作。

允许职责：

- 读取 `2-编导/第N集.md`。
- 统计可能的画面性字段。
- 检查命中字段后是否存在 `镜头语言：`。
- 检查每个镜头语言块中的 `分镜N:` 是否从 1 开始连续编号。
- 输出机械校验报告。

禁止职责：

- 用脚本生成镜头语言正文。
- 用规则拼接替代 LLM 判断节拍点。
- 自动改写原编导稿正文、对白或场景顺序。
