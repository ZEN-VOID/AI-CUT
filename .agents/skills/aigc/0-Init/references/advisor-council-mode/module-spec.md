# 主创会诊模式模块规范（迁移 stub）

本文件已从 `skill-local reference` 降级为迁移 stub，不再承载 canonical 执行合同。

## Canonical Source

- 唯一真源：`.agents/skills/aigc/0-Init/SKILL.md`
- 对应能力面：`Initialization Mode Contract` 中的 `主创会诊模式`
- 对应节点：`N4-mode-engine`

## 保留目的

1. 兼容旧路径引用与历史 evidence path。
2. 明确旧的外置模式合同已经完全内收至父 `SKILL.md`。
3. 防止后续继续在本文件演化出第二真源。

## 使用规则

- 允许读取本文件以发现新落点。
- 不允许继续在本文件补写执行细则。
- 所有新增或修订必须写入父 `SKILL.md`。
