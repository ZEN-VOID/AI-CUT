# Changelog

## 2026-04-26

- 增量吸收 `.agents/skills/aigc/2-编导` 的声画配对、字段纯度、可见/可听/可执行与场景锚点规则，新增 `references/directing-field-projection-bridge.md`。
- 新增可叠加类型包 `types/projection/directing-field-bridge.md`，用于对白、旁白、音效、系统提示和规则文字密集素材。
- 更新 steps、review、templates 与 README，使编导桥接规范进入执行节点、验收门和输出样板，但不改变 `第N组.md` 的唯一主真源地位。
- 升级为完整 Skill 2.0 包结构，补齐 `steps/`、`review/`、`types/`、`knowledge-base/`、`README.md` 与 `CHANGELOG.md`。
- 将根 `SKILL.md` 重写为动态引用入口，保留输入合同、模式选择、引用导航、输出合同、字段映射与根因合同。
- 将思维执行节点迁入 `steps/adaptation-workflow.md`。
- 将质量评分、pass table、validator 与 reviewer 降级口径迁入 `review/review-contract.md`。
- 新增来源类型、改编姿态和输出模式类型包，执行时由 `types/type-map.md` 选择并固定加载。
- 新增 `templates/output-template.md`，与 `SKILL.md` 的 Output Contract 五字段对齐。
