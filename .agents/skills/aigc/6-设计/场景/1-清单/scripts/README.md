# Scripts Boundary

本目录只承载机械辅助脚本或说明文件。

允许：

- 检查 `5-分组/第N集.md` 是否存在。
- 检查分镜组底部 YAML 是否包含 `场景` 字段。
- 检查 `场景清单.md` 是否为三列表格。
- 统计候选来源、重复名称和空字段。

禁止：

- 自动决定别名是否归并。
- 自动决定同一地点不同时段是否拆分。
- 自动决定子空间或跨场景边界。
- 自动生成 `原文描述（关键词式）` 的创作性表述。

任何脚本输出只能作为 evidence 或 warning，由 LLM 完成最终归并裁决和 canonical 落盘。
