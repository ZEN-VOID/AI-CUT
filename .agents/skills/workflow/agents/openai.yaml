interface:
  display_name: "workflow"
  short_description: "HyperFrames-native reference video workflow"
  default_prompt: "Use $workflow to build a HyperFrames-native reference-rhythm video from script, voiceover or transcript, source media, optional reference clips, and a target output directory."
