# CHANGELOG

## 2026-06-05

- Initialized `.agents/skills/lesson/benchmark` as a Skill 2.0 runtime-spine satellite package.
- Added core layout: `SKILL.md`, `CONTEXT.md`, `README.md`, `CHANGELOG.md`, `test-prompts.json`, and `agents/openai.yaml`.
- Defined lesson-specific benchmark boundaries: evidence-backed comparison, gap matrix, tradeoff recommendations, and owning stage improvement route only.
- Kept optional modules uncreated for this turn; no `review/` or `steps/` directories were added.
