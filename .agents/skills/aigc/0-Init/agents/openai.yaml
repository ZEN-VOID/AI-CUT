interface:
  display_name: "AIGC 项目初始化"
  short_description: "用单技能思行网络统筹初始化，把路由、三种模式与充分性审计全部内收于父 SKILL，并写回 north_star、handoff 与项目根治理工件"
  default_prompt: "Use $aigc-init to keep the parent skill as the only canonical writeback owner, lock exactly one init mode, internalize routing, advisor-council, fast-draft, autonomous-qa, and sufficiency-audit capabilities inside the parent SKILL, and synthesize north_star, init_handoff, story-source-manifest, team, project_state, and optional governance-state with a single next-stage recommendation."
