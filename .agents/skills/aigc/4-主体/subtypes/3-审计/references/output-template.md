# 3-审计输出契约

## 固定交付件

每轮 `3-审计` 至少应稳定交付：

1. 结构化失败维度
2. 对应的修订建议
3. 总报告与单主体审计文件
4. `writeback-plan.md`
5. 指向 `2-设计 / 4-面板 / 下游阶段` 的唯一下一入口

## 硬规则

1. 审计终点不是评价好坏，而是给出可执行返工入口。
2. 每条失败维度都必须能回链 `1-清单`、`2-设计`、`3-明细` 或 `2-组间` 的具体约束。
3. 若无需返工，必须明确写“通过并可进入下游”，不得模糊结束。
4. 证据链不足时允许暂缓裁决，不得用主观印象替代依据。
