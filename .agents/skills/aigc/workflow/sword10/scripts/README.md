# Scripts

`sword10/scripts` 仅用于机械辅助，例如生成 dispatch packet、检查输出路径存在、汇总 ledger。脚本不得替代 LLM 主创阶段正文，不得绕过 `2-美学 -> 3-主体 -> 4-编剧 -> 5-导演 -> 6-分镜 -> 7-摄影 -> 8-分组` 的阶段技能合同。

当前包未提供执行脚本；真实运行时由主 agent 或可用 subagent runtime 读取本技能合同后派发。
