# CHANGELOG

## 2026-05-29

- 初始化 `04R-全局优化` 阶段技能包。
- 新增 `SKILL.md`，定义承接 `04-全局预设` 输出物的自然语言驱动二次优化合同。
- 新增高能意图解析机制：`high_energy_intent_map`、`style_taste_profile`、`style_matching_matrix`、`edit_intensity`。
- 新增混合型思行网络：04 源稿锁定、自然语言解析、风格匹配、范围判型、整体/局部/关键词/场景变体优化、schema 复审、写回。
- 新增风格匹配矩阵，要求用户意图与 04 源稿、02 故事源、参考图事实、关键词、负面词和 05/06 下游消费对齐。
- 新增五段式 `SKILL.md Review Gate Configuration`、`Pass Table`、Root-Cause 合同和完成定义。
- 新增 `CONTEXT.md`、`README.md` 与 `agents/openai.yaml`。
