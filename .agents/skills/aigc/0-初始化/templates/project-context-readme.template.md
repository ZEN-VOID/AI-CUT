# CONTEXT

项目：__PROJECT_TITLE__

本目录保存当前项目跨阶段共享的预设包、参考材料索引、风格约束、模型参数记录与非偏好型资料。

## 边界

- 项目偏好、禁区与长期要求写入项目根 `MEMORY.md`。
- live route truth 写入项目根 `STATE.json`。
- 技能经验、失败复盘和跨项目 heuristic 写入对应 skill 的 `CONTEXT.md`。
- 本目录不替代阶段 canonical 输出；阶段真源仍写入 `1-分集/` 到 `9-审片/` 的对应目录。

## 当前内容

- 暂无
