interface:
  display_name: "漫画生成"
  short_description: "读取九刀流提示词 JSON，并通过 Seedream 一次生成 9 张连续竖版漫画页"
  default_prompt: "Use $comic-generation to validate nine_blade_comic_prompts.v1 JSON and call Seedream once with max_images=9 to create exactly 9 separate vertical comic pages, not a collage or nine variants."

