# scripts

`shot-by-shot` 的脚本只允许做机械辅助：

- 检查主报告和项目 `CONTEXT/` 解析文件是否存在。
- 检查 Markdown 是否包含必需标题。
- 统计逐镜表行数、临摹原则数量、禁止照搬项数量。
- 提示缺失字段或阶段解析缺口。
- 检查 `画面风格解析.md`、`编导解析.md`、`摄影解析.md`、`设计解析.md` 的标题与落点。

脚本不得替代 LLM 对视频内容、镜头边界、摄影美学、导演调度、临摹原则或阶段桥接策略的主创判断。
