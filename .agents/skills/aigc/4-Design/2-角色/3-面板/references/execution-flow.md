# Migration Stub

本文件自 `2026-04-12` 起不再承载 `3-面板` 的执行流程真源。

`character_design.json -> prompt整合 / fallback -> template -> packet -> manifest -> handoff` 的完整链路，已内收到 `../SKILL.md` 的：

- `Topology Contract`
- `Thinking-Action Node Network`
- `Convergence Contract`

保留本文件仅用于历史路径兼容。
