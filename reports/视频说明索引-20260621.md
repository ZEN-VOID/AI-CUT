# 视频说明索引报告 - 2026-06-21

## 本轮目标

重命名 `projects/测试/素材/视频` 下所有视频，新增同目录 `视频说明.yaml`，并把 F1 技能升级为会读取和处理该说明文件，以便自动剪辑时能快速判断素材内容与用途。

## 变更摘要

- 13 条视频已统一重命名为语义化英文文件名：
  - `content-*`：AIGC 成片/剧情/战斗素材。
  - `tool-*`：工具层界面、提示词、生成流程、剪辑软件、导入导出等展示素材。
- 新增并升级 `projects/测试/素材/视频/视频说明.yaml` 到 v2：
  - 视频级字段：`media`、`content_profile`、`selection_profile`、`splicing_profile`、`subtitle_safe_zone`。
  - 片段级字段：`segments[].segment_id/start/end/duration_sec/visual_content/semantic_tags/shot_type/motion/action_intensity/text_overlay/best_for/avoid_for/splice_notes`。
  - 当前共 13 条视频、41 个可选片段，可直接辅助 F1 判断选哪段、切多长、如何拼接、字幕放哪里。
  - 保留 `renames.from -> to` 历史映射，仅作审计，不作为可执行路径。
- 同步更新文本引用：
  - 报告、PRP、EDL、渲染命令、项目记录、素材清单中的旧视频路径已更新。
  - 旧视频名相关抽帧/缩略图衍生文件也已同步重命名。
- F1 源层更新：
  - `.agents/skills/workflow/F1/SKILL.md` 新增 `Video Description Manifest Contract`。
  - N1/N5/N7、Convergence、Review Gate、Output evidence 已接入 `视频说明.yaml`。
  - EDL/报告现在要求记录 `video_id/file/segment_id/source_start/source_end/selection_reason/subtitle_risk`。
  - `.agents/skills/workflow/F1/CHANGELOG.md` 和 `test-prompts.json` 已同步更新。

## 验证

- `视频说明.yaml` 解析通过，`videos` 共 13 条，`segments` 共 41 条，均对应真实文件。
- 每条视频的片段时长加总与视频时长一致。
- `test-prompts.json` JSON 解析通过。
- 13 条视频均可被 `ffprobe` 读取。
- 仓库中旧视频名不再作为可执行路径引用；仅保留在 `视频说明.yaml` 的历史 rename 映射中。

## Source Sync Check

- 源层触发：用户要求 F1 技能本身新增 `视频说明.yaml` 读取与处理规则。
- 已同步内容：F1 runtime spine、节点表、验收门、输出证据、CHANGELOG、回归提示。
- 验证结果：YAML/JSON/ffprobe/旧引用扫描均通过。
