[
  {
    "id": "project-governance-status",
    "prompt": "Use $aigc-query to answer the current status and next safe entry for projects/aigc/demo-film.",
    "expected": "Locks PROJECT_ROOT, reads governance/state carriers, distinguishes missing validation evidence, and returns conclusion, evidence paths, gaps, and one next entry."
  },
  {
    "id": "stage-output-validation",
    "prompt": "Use $aigc-query to check whether episode 1 storyboard images in projects/aigc/demo-film have completed validation.",
    "expected": "Reads canonical stage/media carriers plus validation reports, separates file existence from validation pass, and reports checked paths."
  },
  {
    "id": "path-conflict-diagnosis",
    "prompt": "Use $aigc-query to diagnose why an old 5-Image path conflicts with the current 9-图像 runtime path.",
    "expected": "Loads current runtime layout and legacy migration evidence, identifies drift source, and recommends the canonical route without modifying files."
  }
]
