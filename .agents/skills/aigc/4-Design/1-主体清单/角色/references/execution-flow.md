# Migration Stub

本文件自 `2026-04-12` 起不再承载 `1-清单` 的执行流程真源。

原因：

- `1-清单` 已把输入合同、变量判型、思行节点、命令入口与写回规则内收到 `../SKILL.md`
- 当前技能明确采用 `复杂链路的骨架 / 细则分层=false`

若需要当前执行路径、默认落点或命令入口，请直接读取 `../SKILL.md`。

若需要 sibling 共享的输入消费合同，请直接读取：

- `../../_shared/detail-output-consumption-contract.md`

若需要 sibling 共享的对象归一原则，请直接读取：

- `../../_shared/object-normalization-contract.md`
