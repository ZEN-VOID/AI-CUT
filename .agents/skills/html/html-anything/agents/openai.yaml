name: html-anything
description: Codex adapter for Open Design / HTML Anything template-skill selection and HTML artifact projection.
entrypoint: SKILL.md
language: zh-CN
runtime:
  model_role: coding_agent
  authorship: llm_first
permissions:
  filesystem: workspace
  network: not_required_by_default
outputs:
  - html
  - template_selection
  - design_handoff
