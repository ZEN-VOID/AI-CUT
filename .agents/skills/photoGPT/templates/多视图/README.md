# 多视图

多视图模板族用于将参考图和主体描述转化为生产设计页提示词。执行时先判定对象类型，再进入 `场景`、`道具`、`服装` 或 `角色` 子模板。

## 通用约束

- 固定设计页属性，而不是普通海报或故事板。
- 保持同一主体身份、比例、材质、风格和光照逻辑跨视图一致。
- 若需要身份徽章，必须从用户输入或上游设计文档中提取 ID 与名称；无法提取时进入 prompt-only。
- 参照图只提供主体、风格或布局依据，不得偷换上游设计事实。
