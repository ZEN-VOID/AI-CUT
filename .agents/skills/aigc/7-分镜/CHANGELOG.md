# Changelog

## 2026-06-04

- 初始化 `aigc/7-分镜` Skill 2.0 runtime-spine 包。
- 默认承接 `6-氛围/第N集.md`，并允许用户指定 source override。
- 新增 `3-美学/画面基调` 与 `3-美学/分镜风格` 为必读上下文。
- 将旧 `4-摄影` 的画面匹配、节拍、构图、景深、时值、计划汇流和连续性合同重构为 `7-分镜/references/` 下的 4 个专用细则。
- 输出格式固定为 `分镜N（N-N秒）：景别，景深，构图形式，主体陪体背景描述`。
