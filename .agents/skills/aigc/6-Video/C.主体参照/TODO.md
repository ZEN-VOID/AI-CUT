# TODO

- [ ] 如未来要把旧 `bind_reference_assets.py` 移植成本包辅助脚本，必须先保留 `--dry-run`，并确认脚本不承担主体语义裁决。
- [ ] 补充真实项目样例后，可把 subject-index 的字段 schema 独立沉到 `templates/` 或 `references/`。
- [ ] 若 provider 层新增本地可执行技能，更新 `provider-handoff-contract.md` 的 provider-specific 解析示例。
