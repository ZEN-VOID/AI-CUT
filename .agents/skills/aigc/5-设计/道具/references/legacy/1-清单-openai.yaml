version: 1
interface:
  display_name: "5-设计 / 道具清单"
  short_description: "从 3-Detail episode JSON 提取道具清单、道具研究与 prop design bridge 三真源。"
  default_prompt: "Use $aigc-design-prop-list to read `projects/aigc/<项目名>/3-Detail/第N集.json`, lock canonical `meta + groups[].global/detail.分镜列表` as upstream truth, treat compat `分镜组列表 / 分镜明细 / 运动表现 / 视觉强化 / 道具及状态` as derived evidence only, and only fall back to legacy `projects/aigc/<项目名>/编导/第N集.json` when needed, then write `道具清单.json + 道具研究.json + prop_design_bridge.json` plus `_manifest.json` under `projects/aigc/<项目名>/4-设计/道具/1-清单/第N集/`."
