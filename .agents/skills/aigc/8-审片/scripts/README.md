# Scripts Boundary

本目录只承载审片机械辅助脚本。允许脚本做：

- 视频元数据读取。
- 关键帧抽取。
- 音频强度统计。
- 文件名和路径结构检查。
- 审片报告字段完整性检查。

脚本不得替代 LLM 判断：

- 不得自动生成审片结论。
- 不得自动决定是否修 `4-分组`。
- 不得自动上升源层优化。

当前包先以 shell / ffmpeg / ffprobe 辅助为主，后续若沉淀稳定检查项再新增脚本。
