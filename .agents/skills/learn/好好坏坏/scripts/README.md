# Scripts

`好好坏坏` 的 `scripts/` 只承载机械辅助，不拥有源层诊断、审美判断、叙事判断或调优裁决权。

允许的脚本职责：

- 读取目标 skill 文件清单。
- 对好/坏示例做文本 diff、字段统计或 schema 校验。
- 检查 Markdown 链接、路径引用、JSON/YAML 格式。
- 执行 dry-run 验证和报告格式投影。

禁止的脚本职责：

- 自动生成“好在哪里、坏在哪里”的核心判断。
- 用启发式规则替代 LLM 的源层 owner 裁决。
- 直接生成创作正文、研究结论、提示词、设计稿或审美/叙事内容。
