interface:
  display_name: "AIGC 项目查询"
  short_description: "查询项目治理状态、断点、阶段产物与运行时证据"
  default_prompt: "Use $aigc-query to locate the real PROJECT_ROOT, read governance-state plus project-state carriers, and answer with evidence-backed AIGC project facts."
