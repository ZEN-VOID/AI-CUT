# Default Type Package

This is the starter subtype package for `text-to-speech`.

## Purpose

- Keep `types/` non-empty for initial Skill 2.0 validation.
- Replace this starter package with 3-5 concrete subtype packages when the skill's business object is clear.

## Match Signals

- Use only as a temporary fallback before concrete subtype packages are defined.

## Fixed Context

- Load this file only when no more specific subtype package exists.

## Replacement Gate

- Before production use, replace `types/default/default.md` and the matching `types/type-map.md` row with concrete subtype packages.
