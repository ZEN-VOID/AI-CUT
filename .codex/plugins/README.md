# Plugins

Store local Codex plugin projects here when this repository needs plugin-scoped skills, apps, or MCP integrations.

Recommended layout:

```text
plugins/
  my-plugin/
    .codex-plugin/
      plugin.json
    skills/
    assets/
```

Use `../templates/plugin/.codex-plugin/plugin.json` as a starter manifest.
