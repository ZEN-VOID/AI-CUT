interface:
  display_name: "AIGC 4-Design"
  short_description: "统一路由场景、角色、服装、道具的清单、设计与面板三段设计阶段，并在需要时只消费前置监制 advisory。"
  default_prompt: "Use $aigc-design to route the current task into `1-清单`、`2-设计` or `3-面板`, dispatch only the needed design domains under `projects/aigc/<项目名>/4-Design/`, keep the parent skill as the only stage-level router and summary owner, and if project-root `team.yaml` enables `roles.supervision`, consume that role only as pre-draft advisory. Do not run post-write supervision refine from the `4-Design` parent contract."
version: 1
