# TODO

- [x] Create Skill 2.0 canonical partitions for `0-Init`.
- [x] Move long initialization contracts out of `SKILL.md` into owner partitions.
- [x] Add `README.md`, `TODO.md`, and product metadata alignment.
- [ ] Add a dedicated mechanical dry-run helper for project skeleton validation if repeated manual checks keep recurring.
- [ ] Extend `scripts/aigc_skill_audit.py --strict` if it needs deeper checks for the new `references/` partition split.
