# Changelog: aigc 2-美学/分镜风格

## 2026-06-15

- 修订分镜 8 维：补入 `注意力/视点路由` 与 `信息揭示/声画节拍`，将下游容错并入段落推进维度。
- 在信息密度校准节点加入对白、音效、静默和情绪反应对镜头切换节奏的影响。
- 修正父级整体调用下的并发语义：缺失 `画面基调` 或 `摄影风格` 时标记 `dependency_gap`，不得降级为串行链。

## 2026-06-11

- 增加逐集输出 scope：单集来源或目标写入 `2-美学/第N集/分镜风格/分镜风格协议.md` 和同目录 `执行报告.md`；整季/项目基线保留项目级路径。
- 同步 `SKILL.md`、`CONTEXT.md`、`README.md` 与 agent prompt 的输出路径说明。

## 2026-06-04

- 创建 Skill 2.0 runtime-spine core layout。
- 新增 `SKILL.md`，定义分镜风格的业务边界、输入路由、节点表、Mermaid 图、模块授权、审查门、输出合同和学习回写规则。
- 新增 `CONTEXT.md`，沉淀分镜正文越权、剧情代写、摄影边界、参考视频照搬和下游不可执行等修复经验。
- 新增 `README.md`、`agents/openai.yaml` 和 `test-prompts.json`。
