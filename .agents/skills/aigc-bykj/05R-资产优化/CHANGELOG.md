# CHANGELOG

## 2026-05-29

- 初始化 `05R-资产优化` 为 BYKJ AIGC 工作流的资产二次优化阶段。
- 明确承接 `05-资产提取` JSON 产物，不重新提取、不制造无证据资产。
- 增加自然语言资产意图解析、角色/场景/道具分支优化、跨资产一致性修复、schema/template 修复和 prompt 修复合同。
- 固定 JSON-first 输出：patch、优化报告、manifest，并按需输出 full optimized JSON。
- 增加 `CONTEXT.md` 经验层和 `agents/openai.yaml` 入口元数据。
