# CONTEXT.md

## Purpose & Loading Contract

- 本文件是 `aigc/5-设计/道具` 的经验层知识库。
- 调用 `.agents/skills/aigc/5-设计/道具/SKILL.md` 时，应自动预加载本文件。

## Context Health

- 当前文件只保存道具设计域的经验，不承载项目级道具正文、设计总稿或 provider 运行日志。
- 若经验影响整个 `5-设计` 阶段，应晋升到父级 `CONTEXT.md` 或 `SKILL.md`。

## Reusable Heuristics

- 道具设计应先锁功能、材质、时代/世界观位置和镜头可读性，再进入生成请求。
