model: gpt-5
metadata:
  short_description: "围绕固定 `剧本正文` 生成 `角色表现 / 运动表现 / 氛围表现 / 视觉强化` owner bundle，输出到 `3-Detail/水月/第N集.field-patch.json`"
  default_prompt: "Use $aigc-detail-watermoon as the stage-local child skill under `3-Detail`: lock `projects/aigc/<项目名>/1-Planning/3-分组/第N集.md` plus `projects/aigc/<项目名>/3-Detail/第N集.json` as the only truth sources, preserve `剧本正文` exactly as inherited, treat shared-root `分镜切换` as upstream fixed context rather than something to rewrite, run `1-角色表现 -> 2-运动表现 -> 3-氛围表现 -> 4-视觉强化` in current numeric order, reread the current partially written root before each next branch, emit branch process sidecars plus an `assembly_only` owner bundle whose canonical truth is `分镜明细[].角色表现 / 运动表现 / 氛围表现 / 视觉强化`, and treat any legacy `角色背景面 / 角色站位走位 / 道具及状态 / 分镜表现` output only as compatibility projection."
