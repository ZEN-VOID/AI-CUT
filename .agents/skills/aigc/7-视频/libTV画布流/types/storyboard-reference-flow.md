# Type Package: storyboard_reference_flow

分镜参照流当前为空白占位，待后续补充。

## Fixed Context

- 只有用户显式指定“分镜参照流 / storyboard reference flow / 用分镜图或故事板图参照出视频”时才选择本包。
- 当前不得向 LibTV 提交远端生成任务。
- 当前输出应说明：该路线已占位，后续需要补充输入对象、图像参照绑定、LibTV handoff、质量门禁和输出目录。

## Placeholder Gate

返回 `not_implemented_placeholder`，并建议用户改用默认 `subject_reference_flow` 或等待补充分镜参照流细则。
