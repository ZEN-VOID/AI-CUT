# CONTEXT.md

## Purpose & Loading Contract

- 本文件是 `aigc/2-编导` 的经验层知识库，不是过程日志。
- 调用本目录 `SKILL.md` 时必须同时加载本文件。
- 本文件不改写 `SKILL.md` 的输入、输出和门禁；只沉淀可复用判断经验、失败模式和修复打法。

## Context Health

- soft_limit_chars: 18000
- hard_limit_chars: 36000
- status: ok
- last_checked_at: 2026-04-25

## Type Map

| failure_or_outcome_type | root_cause_layer | immediate_fix | systemic_prevention | verification_point |
| --- | --- | --- | --- | --- |
| 编导稿变成剧情摘要 | 输出真源层 | 回到上游逐段承接，把被压缩事实恢复为字段化正文 | 在 review gate 固定“不得摘要、不得删减、不得重排” | 抽样段落能回指上游原文事实与顺序 |
| 对白被润色或语序调整 | 对白冻结层 | 用上游原句逐字替换输出对白 | 对所有引号对白执行人工或脚本抽查 | 对白文本与上游一致，且动作不在引号内 |
| 声画字段混写 | 字段纯度层 | 拆成声音本体与对应 `*画面` 两行 | 按字段路由表写作，声音行不解释画面，画面行不复述声音 | 每条对白/独白/旁白/音效就近有对应画面字段 |
| `动作画面` 写成小说内视 | 可拍性层 | 改成身体动作、空间运动、表演或镜头可见承托 | 内心、判断、恐惧优先进入独白/心理反应/表演提示 | `动作画面` 中不出现“意识到/觉得/知道/没人知道”等抽象叙述 |
| 相同地点因剧情 beat 反复开新场景 | slugline 稳定层 | 合并到首次 slugline 场景下，后续 beat 直接接正文 | 建立同集 slugline 表，只因日/夜或真实空间变化新开场景 | 同一 slugline 在单集中只出现一个场景编号 |
| 好莱坞质量被误解为自由改写 | 质量优先级层 | 只提升场景目的、动作选择、画面可拍性和节奏，不改剧情因果 | 在 quality spec 固定“保真优先于漂亮改写” | 高质量字段仍能逐项回指上游事实 |
| 高潮画面被压平成普通叙述 | 高点识别层 | 回到上游锁定行动结果、认知翻转、关系暖点、规则显影、奇观或怪异落点，再用既有画面/声音/表演字段强化 | 在字段分流后固定 `peak_visual_pass`，但禁止新增事实、对白或事件结果 | 高点能回指上游，输出有可拍承托和状态差 |
| subagents 启用时只本地模拟顾问 | 顾问请教层 | 回到 `team.yaml` roster，按共享团队顾问合同真实 dispatch 或写明上层阻断降级 | 把 `advisor_consultation_packet` 固定为 LLM 投影前上下文，不让顾问替代上游真源 | 执行报告可看到 roster 来源、问题类型、可执行指导或降级说明 |
| 剧本化被误解为抽象解释 | 具像化层 | 把 `画面/表演/动作/环境/道具` 中的概念、比喻、心理结论改为身体、物件、文字、光线、空间和构图 | 在字段合同中固定“可见/可听/可执行”定义与反抽象门禁 | 随机抽查 `*画面` 字段能回答“摄影机看见什么” |
| 音效字段写成事件说明 | 声音本体层 | 把“八点四十分铃声”“一声短促闷响”等改成拟声或可听文本，把时间/来源放到画面或上下文 | validator 增加音效描述性禁词 | `音效` 引号内不出现“铃声/声音/一声/响起/传来”等说明词 |
| review 后直接交付未修稿 | 末段闭环层 | 回到 `N6R-DIRECT-REPAIR`，按 finding 最小修字段、声画、slugline、具像化或报告证据，再复审 | `Stage-End Review-Repair Contract` 固定 candidate -> review -> repair -> re-review -> writeback | 执行报告有 repair_actions 与 re_review_verdict，canonical 稿无阻断项 |

## Repair Playbook

1. 先锁定 `source_episode_path`，确认输出对应的上游 `第N集.md`。
2. 抽取上游 `【剧本正文】` 后正文，按原顺序建立事实承接清单。
3. 检查所有中文双引号里的对白，逐字回对上游原句；没有上游依据的新增对白必须删除或改为非 canonical 候选说明。
4. 检查每个声音字段后是否就近出现对应画面字段；缺失时补画面承托，不把声音文本复述到画面行。
5. 检查 `动作画面`、`角色动作` 是否可拍；心理和判断转入 `独白`、`心理反应`、`表演提示`。
6. 建立 slugline 去重表，合并同一集内完全相同的场景标题。
7. 最后才做编剧质量 polish：强化场景目的、冲突压力、表演任务和可见承托，但不改变事实和对白。
8. 执行一次“高潮画面 pass”：若上游存在行动结果、认知翻转、关系暖点、规则显影、奇观、怪异落点或高超对决，至少让一个高点落入强画面字段和必要声音/沉默承托；没有真实高点时只处理最强 `micro_payoff`，不硬造大高潮。
9. 若本轮启用 subagents，先把 `team.yaml` 监制顾问的脑洞汇成可执行指导：问结构、表演、声画承托、高潮和保真风险，不问泛泛“好不好”。
10. 交付前专门执行一次“画面纯度 pass”：所有 `*画面`、`环境描写`、`道具特写`、`表演提示` 必须落到可见或演员可执行事项；抽象信息转入 `心理反应`、`独白`，或删除。
11. 交付前专门执行一次“声音本体 pass”：所有 `音效` 引号内必须是可听见的声音，不得是时间、事件或描述性概括。
12. 交付前把 review finding 当成修复输入，而不是报告附件；阻断项先在本阶段修复并复审，仍失败再阻断或回源层。

## Reusable Heuristics

- `2-编导` 的“创作”主要发生在投影方式、画面承托、场景标题和表演调度上，不发生在剧情事实重写上。
- 好的影视化不是把小说写短，而是把小说信息拆成可拍字段，让下游能解析、调度和表演。
- 声音字段要像录音部门能拿走的材料，画面字段要像导演和摄影能拍出来的材料。
- 角色内心可以保留，但要转成有主体的独白、心理反应或表演提示；不要假装内心解释是可拍动作。
- slugline 是下游分组的锚点，不是剧情 beat 标题；同一个空间和时间段内只需要一个场景编号。
- 质量提升的顺序是：保真 -> 可拍 -> 声画清楚 -> 表演可执行 -> 下游可分组。
- 团队顾问的价值是“创作前照亮取舍”，不是接管剧本；最终执行口径仍要落成字段、表演任务、声画承托和风险禁区。
- 高潮画面强化的顺序是：先找上游已有满足兑现点，再找角色锚点和状态差，最后才强化画面、声音、表演和余波；不要为了“更炸”新增事实。
- `2-编导` 不使用 `镜头语言预设` 字段；导演意图必须落到画面、动作、表演或声音字段，摄影/分镜方案交给下游阶段。
- “画面”不是解释画面的意义，而是把意义放进可见物：手、眼、门、纸、黑板、光线、座位、空位、屏幕字、身体距离。
- “音效”不是声音的说明书；把“八点四十分”这类信息放到画面上下文，把引号内留给真正能听见的声音。
- `2-编导` 的终稿感来自阶段内闭环，不来自另开二改阶段：candidate 必须被审计、修复、复审后才交给 `3-摄影`。
