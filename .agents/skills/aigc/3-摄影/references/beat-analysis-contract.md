# Beat Analysis Contract

本文件定义 step2“节拍分析”。一个节拍点对应一个分镜切换点，最终表现为 `分镜N:`。

## Core Principle

节拍点不是句号、逗号或均匀切块，而是观众注意力需要被重新组织的瞬间。判断标准是：若继续用同一机位、同一景别、同一运动和同一焦点会损失信息、情绪、压迫或动作清晰度，就产生一个新的分镜切换点。

## Beat Trigger Matrix

| trigger_id | 节拍触发 | 判断问题 | 常见镜头响应 |
| --- | --- | --- | --- |
| `BT-01` | 主体切换 | 观众注意力是否从人物转向物、从群体转向个体、从前景转向背景？ | 景别切换、焦点转移、反打、插入特写 |
| `BT-02` | 动作分相 | 动作是否包含预备、执行、结果、反应多个阶段？ | 连续分镜、跟拍、甩镜、动作匹配剪辑 |
| `BT-03` | 信息揭示 | 是否有文字、道具、眼神、异常细节首次变得可读？ | 推轨、微距、焦点拉移、显影式特写 |
| `BT-04` | 情绪转折 | 角色表情、身体、呼吸、沉默是否发生压力变化？ | 由宽到近、压缩焦段、停顿长镜、反应镜头 |
| `BT-05` | 空间关系 | 角色和危险源、门窗、讲台、黑板、座位的关系是否需要重新定位？ | 建立镜头、俯拍、轴线重置、遮挡构图 |
| `BT-06` | 声画驱动 | 声音是否先行、骤停、变形或把注意力牵到画外？ | 声音先行转场、切反应、空镜承压 |
| `BT-07` | 视觉形态变化 | 光、色、文字、物体状态是否发生可见变形？ | 匹配剪辑、形态转场、光变剪辑、显影推进 |
| `BT-08` | 权力关系变化 | 谁控制场面、谁被凝视、谁被孤立是否改变？ | 高低机位、对称构图破坏、压迫式前景 |
| `BT-09` | 摄影参数变化 | 景别、景深、视角、镜头类型或运镜速度是否需要改变才能更清楚？ | 从全景切特写、深景深切浅景深、固定机位切手持 |

## Decision Procedure

1. 先写出当前画面句子的戏剧任务：建立空间、执行动作、显影信息、制造恐惧、转交注意力或完成反应。
2. 标记句子中的主体、动作动词、可见物、空间锚点、光色变化和反应对象。
3. 套用 `Beat Trigger Matrix`，只保留会改变观看策略的触发点。
4. 合并弱节拍：如果两个触发点可以在同一镜头内用焦点拉移、演员走位或景深变化完成，不必硬切。
5. 拆开强节拍：如果同一镜头会让动作、信息或情绪互相遮蔽，就拆成新的 `分镜N`。
6. 为每个保留节拍选择最小充分摄影参数：景别、景深、镜头视角、镜头类型、运镜速度。
7. 将参数组织成动态路径：从哪里开始、怎么变化、速度如何、落到哪里。
8. 输出分镜数量时遵守：少则精准，多则有因；不以炫技数量替代节拍判断。

## Beat Density Guide

| visual_unit_type | default density | split when |
| --- | --- | --- |
| 环境/空间建立 | 1-2 个分镜 | 空间中有隐藏威胁、视线转移或重要物件 |
| 单一身体动作 | 1-3 个分镜 | 动作有预备、冲击、结果或反应 |
| 道具/文字/规则显影 | 1-3 个分镜 | 需要从环境转入可读细节，再转到角色反应 |
| 群像混乱 | 2-4 个分镜 | 人群中存在多个注意力焦点或情绪波峰 |
| 表情/表演提示 | 1-2 个分镜 | 微表情与外部压力源需要互相反打 |
| 高能转场节点 | 1-2 个分镜 | 形态、声音、运动方向或光色能把两个画面咬合 |

## Shot Count Cardinality Guard

分镜数量必须由当前 `visual_unit` 的真实节拍触发决定，不能被模板、上一条输出习惯或批量生成惯性推成固定数量。

- `1 个分镜` 是合法且常见的结果：低信息过场、单一表演承托、单一文字读秒、单一反应镜头，只要一个镜头能完成观看策略，就不得硬补 `分镜2`。
- `2 个分镜` 只在存在第二个真实观看策略时成立，例如建立后揭示、动作后反应、环境后可读细节、主体后压力源、声画接口后落点。
- `3-4 个分镜` 只给关键规则显影、动作分相、群像恐慌、高潮/爽点/高光、空间重置或强断裂转场；每一镜都必须有新主体、新动作相位、新信息、新情绪压力、新空间关系或新交接接口。
- 同一集或同一场中若大量 `visual_unit` 都输出相同分镜数，尤其 2 镜占绝对多数，必须视为 `beat_map` 塌缩风险：抽样复判低信息块是否应收敛为 1 镜，关键块是否被压平而需要 3-4 镜。
- 审查时不以“数量越多越好”或“2 镜更完整”为标准，只问：删掉这一镜，观众是否少看见、少理解或少感受到必要内容。

## Anti-Patterns

- 按固定三分镜模板硬套所有画面句子。
- 按固定二分镜模板硬套所有画面句子。
- 把同义的构图描述拆成多个伪节拍。
- 每个动作都切特写，导致空间关系丢失。
- 为了转场炫技牺牲演员停顿和恐惧延迟。
- 只写“镜头推进”“氛围压迫”，但没有说明为何在此处切换。
