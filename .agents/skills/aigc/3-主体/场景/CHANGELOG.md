# CHANGELOG

## 2026-06-16

- 升级 `$aigc-design-scene` 父包到 runtime-spine Skill 2.0 口径。
- 补齐业务画像、类型路由、节点表、模块矩阵、触发矩阵、收敛门、review gate、量化、注意力、检查点与学习回写。
- 新增父包 `agents/openai.yaml`、`README.md` 和 `test-prompts.json`。
