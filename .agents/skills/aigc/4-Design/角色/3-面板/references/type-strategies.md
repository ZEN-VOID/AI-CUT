# Migration Stub

本文件自 `2026-04-12` 起不再承载 `3-面板` 的类型策略真源。

`Design Subject Strategy`、`Role Tier Strategy`、`Reference Image Strategy` 与 `Conflict Tie-Break`，均已内收到 `../SKILL.md` 的 `Topology Contract`。

当前技能已固定：

- `复杂链路的骨架 / 细则分层=false`
- 类型策略必须跟节点网络与输出合同同源

如需当前执行规则，请回到 `../SKILL.md`。
