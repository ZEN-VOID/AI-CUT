interface:
  display_name: "AIGC 项目初始化"
  short_description: "用单技能单一真源目录统筹首次初始化或重置式重新初始化；路由、三种模式、rebootstrap 边界与充分性审计全部内收于父 SKILL，不再保留平行 mode reference 目录"
  default_prompt: "Use $aigc-init to keep the parent skill as the only canonical writeback owner, lock exactly one init mode, internalize routing, advisor-council, fast-draft, autonomous-qa, rebootstrap-reset, and sufficiency-audit capabilities inside the parent SKILL, avoid recreating parallel mode reference directories, and synthesize north_star, init_handoff, story-source-manifest, team, project_state, and optional governance-state with a single next-stage recommendation."
