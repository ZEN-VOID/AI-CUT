interface:
  display_name: "AIGC 5-Image 分镜故事板"
  short_description: "把分镜组蒸馏为多格 storyboard 图像请求 JSON"
  default_prompt: "Use $aigc-storyboard-sheet to follow the N0-N6 thinking-action network, read `projects/aigc/<项目名>/3-Detail/第N集.json`, lock one canonical storyboard group, assemble `storyboard_group`, preserve the fixed English prefix, fill the shared image request template, and write the canonical `第N集.json` under `projects/aigc/<项目名>/5-Image/分镜故事板/` without generating images or rewriting source facts."
