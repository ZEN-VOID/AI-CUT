# Changelog

## 2026-06-10

- 新增 `分镜平面图` 叶子路线，用于组级顶视图角色站位、动线、机位和空间连续性平面图。
- 同步父级路由、Mode Selection、Type Routing、Reference Loading Guide、Module Loading / Trigger、Visual Map、Output Contract 与 CONTEXT 类型经验。

## 2026-06-04

- 将 `9-图像` 项目输出目录从旧业务前缀 `A-分镜画面` / `B-分镜故事板` 收敛为与真实叶子技能包名一致的 `分镜画面` / `分镜故事板`。
- 同步父级 `Output Contract`、Visual Map、registry route、query runtime layout 和审计脚本中的图像阶段路径引用。
