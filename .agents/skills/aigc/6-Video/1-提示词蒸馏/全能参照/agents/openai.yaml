interface:
  display_name: "AIGC 全能参照"
  short_description: "把编导分镜组蒸馏为视频请求 JSON、TXT 与 manifest"
  default_prompt: "Use $aigc-video-seedance2 to read `projects/<项目名>/3-Detail/第N集.json`, distill each 分镜组 into the canonical video request JSON, preserve `剧本正文` and `全局风格` verbatim, write `第N集.json`, `第N集.txt`, and `_manifest.json`, and keep `SKILL.md` as the only contract source."
