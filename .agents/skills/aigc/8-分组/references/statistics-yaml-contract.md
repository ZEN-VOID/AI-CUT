# Statistics YAML Contract

本文件定义每个分镜组底部 YAML 统计的字段、统计边界和抽取规则。

## YAML Shape

每个分镜组底部必须附加 fenced YAML：

```yaml
字数统计: 0字
时长估算: 约0秒
角色:
  - id: CHR-001
    name: 角色标准名
场景:
  - id: SCN-001
    name: 场景标准名
道具:
  - id: PRP-001
    name: 道具标准名
```

无角色、场景或道具时使用空列表 `[]`，不得省略字段。

`角色`、`场景`、`道具` 的非空项必须来自 `projects/aigc/<项目名>/3-主体/subject-registry.yaml`：

- `角色` 对应 `subjects.characters`，ID 使用 `CHR-###`。
- `场景` 对应 `subjects.scenes`，ID 使用 `SCN-###`。
- `道具` 对应 `subjects.props`，ID 使用 `PRP-###`。
- `name` 必须等于 registry 的 `canonical_name`；若正文中使用别名，只能在执行报告记录归一化，不在 YAML 内写别名。
- 旧稿中只有字符串项时，视为 legacy candidate；正式 pass 前必须归一为 `{id, name}`，或在执行报告中说明只做兼容回读。

## Count Boundary

`字数统计` 统计该分镜组标题后重复输出的场景标题行，以及从 `7-摄影` 或用户指定 source 划定并同步原换行的分镜正文，包括正文内场景标题、字段名、对白标签、分镜行标题和时间码内容。

以下结构不计入 `字数统计`、`时长估算` 或约 1680 字语义复核参考线：

- 分镜组标题。
- `全局风格：` 字段标题与字段内单行 `Global Style Prompt` 整理内容。
- 组底 fenced YAML。
- 独立 `## <上一个分镜组ID>~<下一个分镜组ID>` 连接件块禁止输出；若旧稿残留，必须删除，且不计入任何分镜组统计。

`时长估算` 取当前分镜组累计时间码中最后一个 `[N-N秒]` 的结束秒。落盘稿中的每个分镜行不再各自从 `[0-N秒]` 重启，而是按当前分镜组从 0 秒开始连续递增，后一个时间段起点必须等于前一个时间段终点；最终累计结束秒必须以 `.5` 结尾，若自然相加不是 `.5` 结尾则在组尾上调 0.5 秒，YAML 回指这个最终结束秒。组标题、标题后重复输出的场景标题行、`全局风格：` 和组底 fenced YAML 均不计入。若上游缺少 canonical 时间段，应回到 `7-摄影` 补齐；临时兼容 legacy 旧稿时，执行报告必须说明缺少时间段来源和估算风险。

## Role Extraction

角色字段优先来源是 `3-主体/subject-registry.yaml`，分组正文只用于判断本组是否实际出现该注册角色。出现判定参考：

1. `对白（角色名）`、`独白（角色名）`、`旁白（角色名）` 等声音主体。
2. `角色动作：`、`表演提示（角色名）：`、`表情特写：` 中明确出现的人名。
3. 第一个普通分镜行或场景正文中明确可见或可听见的角色名。

群像必须先在 registry 中登记为稳定主体，例如 `学生群像`、`围观人群`。未实际出场但被提及的角色不列入，除非本组以影像、声音或道具显影方式让其成为生成主体，且该主体已在 registry 中存在。

## Scene Extraction

场景字段优先从 registry 中选取与当前 `### 场景N：...` 场所名对应的 `SCN-###`。一般一个分镜组只对应一个场景；若跨场景，优先重新拆组。确需多场景时，按出现顺序列多个已登记场景，并在执行报告说明原因。不得为了适配场景标题在 YAML 中临时创造新场景名。

## Prop Extraction

道具字段只列 registry 中已登记、且在本组承担重要叙事道具、规则道具、视觉钩子或后续生成锁定功能的物件。普通环境物件只有在本组承担叙事功能且已登记时才列入；未登记物件应回到 `3-主体` 作为 registry repair 候选。

道具抽取必须先做同物识别与归一合并，再写入 YAML：

- 同一物品只列一次，使用最稳定、最利于后续资产锁定的称谓；不要把颜色、位置、破损状态、持有人或镜头景别拆成多个道具。
- 形容词差异、状态差异、持有人差异、镜头差异、单复数差异和局部部件默认合并到同一 canonical 道具名，例如 `红色手机` / `手机屏幕` / `她的手机` 可合并为 `手机`，除非正文明确存在多部不同手机且会影响生成一致性。
- 若同一大件的部件本身承担独立叙事功能，才可单列部件；否则部件并入主体物件。
- 同类多个普通背景物不逐项铺开；需要保留群体功能时写成集合称谓，例如 `课桌椅`、`散落文件`、`监控屏幕组`。
- 道具名应短而稳定，避免写成动作句、镜头句或状态句；状态变化写在正文或执行报告中，不在 YAML 道具列表中重复展开。

优先来源：

1. `道具特写：`
2. `规则显影：`
3. `系统画面：`
4. 第一个普通分镜行中明确可见或被人物互动的关键物件
5. 对白或动作中被明确使用的关键物件

旧连接件中的关键视觉承接点不得污染本组正文统计；若该物件也在本组正文或第一个普通分镜行中出现，按正文证据列入。

## Review Notes

YAML 是统计证据和机器可读索引，不是新的剧情真源，也不是分镜组开头的主体信息展示层。角色、场景和道具必须同时满足：已存在于 `3-主体/subject-registry.yaml`，并能回指本组正文或第一个普通分镜行。YAML 只保留 registry 的 ID 与 canonical name。

不得为了补充主体信息，在组头或 YAML 前额外输出 `角色：` / `场景：` / `道具：` 结构化清单。若需要更准确地传达主体属性，应回到上游 `7-摄影` 正文或第一个普通分镜行中补足可回指信息。

每个 YAML 统计项都应能回指本组正文、场景标题、对白主体、分镜行、道具特写、规则显影或系统画面中的证据。`时长估算` 应能回指组内累计时间码的最后结束秒，且该结束秒必须以 `.5` 结尾。

未登记主体处理：

- 不能在 `8-分组` 内新增主体名称。
- 明确是 registry 漏项时，分组阶段记录 `unregistered_subjects`，返工到 `3-主体` 补 `subject-registry.yaml` 和对应清单。
- 不是生成主体时，从 YAML 中删除，只保留在正文叙事中。

## Review Gate Mapping

| Review Question | Review Gate | Fail Code | Rework Target | Report Evidence |
| --- | --- | --- | --- | --- |
| 每个分镜组底部是否都有 fenced YAML，且包含 `字数统计`、`时长估算`、`角色`、`场景`、`道具` 五个字段；空列表是否写为 `[]` 而非省略？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `YAML Shape` | 执行报告记录每组 YAML 字段完整性、空列表规范和缺字段修复结果。 |
| `字数统计` 是否只统计标题后的场景标题行和同步原换行的分镜剧本正文，并排除组标题、`全局风格：` 与 YAML fenced block？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `Count Boundary` | 执行报告记录字数统计口径、计入/排除项和 validator 或人工估算差异。 |
| `时长估算` 是否取当前分镜组累计时间码最后结束秒；落盘稿中的 `[N-N秒]` 是否从 0 秒开始连续递增，没有在后续分镜行中重启为 `[0-N秒]`，最终结束秒是否以 `.5` 结尾，也没有把组头、全局风格或 YAML 计入？ | `GATE-GROUP-06`、`GATE-GROUP-11` | `FAIL-GROUP-05`、`FAIL-GROUP-08` | `N3-GROUP-BOUNDARY`、`N7-ASSEMBLE-STATS`、本文件 `Count Boundary` | 执行报告记录每组累计时间码、最后结束秒、`.5` 收束和排除项核对。 |
| `角色` 是否只列本组实际可见、可听见或以影像/声音/道具显影成为生成主体的角色/群像，未把纯提及人物列入？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `Role Extraction` | 执行报告记录角色项、对应对白/动作/显影证据和删除的无出场人物。 |
| `场景` 是否优先取当前 `### 场景N` 场所名；若出现多场景，是否拆组或在报告中说明确需多场景的原因？ | `GATE-GROUP-05`、`GATE-GROUP-11` | `FAIL-GROUP-04`、`FAIL-GROUP-08` | `N2-SOURCE-INVENTORY` / `N3-GROUP-BOUNDARY`、`N7-ASSEMBLE-STATS`、本文件 `Scene Extraction` | 执行报告记录场景项、上游场景标题来源、多场景组原因或拆组结果。 |
| `道具` 是否只列重要叙事道具、规则道具、视觉钩子或后续生成必须锁定的物件，未把普通环境背景物过度列入？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `Prop Extraction` | 执行报告记录候选道具、删除的普通背景物和保留道具的叙事/规则/生成理由。 |
| 道具是否已完成同物识别、归一合并和去重，未因颜色、位置、状态、持有人、镜头景别、单复数或普通部件拆成多个 YAML 项？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `Prop Extraction` | 执行报告记录合并前候选、canonical 道具名、同物异名/部件合并和保留拆分理由。 |
| YAML 是否只作为统计证据和机器索引，没有承载站位段落、形容属性或替代组头主体信息；组头也没有额外输出 `角色：` / `场景：` / `道具：` 清单？ | `GATE-GROUP-11` | `FAIL-GROUP-08` | `N7-ASSEMBLE-STATS`、本文件 `Review Notes` | 执行报告记录 YAML 短名单检查、组头清单禁用检查和正文证据回指结果。 |
| YAML 的 `角色`、`场景`、`道具` 是否全部能以 `id + name` 命中 `3-主体/subject-registry.yaml`，没有在分组阶段新增主体信息？ | `GATE-GROUP-SUBJECT-REGISTRY` | `FAIL-SUBJECT-REGISTRY-UNREGISTERED` | `N7-ASSEMBLE-STATS`、回到 `3-主体` registry repair | 执行报告记录 `subject_registry_alignment`、归一化项、未登记项和返工目标。 |
