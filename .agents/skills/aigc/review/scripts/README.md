# Scripts

本目录保留 `aigc-review` 的包内机械辅助脚本说明。当前 canonical runner 位于仓库根：

```bash
python3 scripts/aigc_review_runner.py --help
```

脚本只负责 fact pack 写入、provider bridge、packet 聚合、repair sidecar 与治理桥接；不得替代阶段技能进行创作或业务 truth 改写。

