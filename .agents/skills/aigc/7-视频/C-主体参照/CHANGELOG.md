# Changelog

## 2026-04-30

- 将 LibTV 视频生成默认路由改为以 `.agents/skills/cli/libTV` 后端默认路由为中心。

## 2026-04-26

- 初始化 `7-视频/C-主体参照` Skill 2.0 包。
- 固化 `4-分组` 作为视频 prompt 主源，组底 YAML 作为主体参照唯一默认基准。
- 增加 $libTV skill scripts handoff、后台并发批量提交、queue ledger 和项目内输出合同。
