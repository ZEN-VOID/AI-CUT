# Changelog

## 2026-06-16

- Created `$fine-tuning` as an AIGC `2-10` iterative tuning satellite skill.
- Added runtime-spine `SKILL.md`, context knowledge base, stage-specific tuning schemes, type package, review contract, report template, local validator, agent metadata, and test prompts.
