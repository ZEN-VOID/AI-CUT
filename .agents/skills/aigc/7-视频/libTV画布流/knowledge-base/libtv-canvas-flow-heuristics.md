# LibTV Canvas Flow Heuristics

- 画布流最容易失败的点是主体图错位；每次多主体任务都应把 `主体绑定表` 放进远端消息。
- 同一项目的主体图通常已经在画布上，不需要每条视频重新上传；但必须能拿到 `node_key + URL`。
- `4-分组` 的组正文比二次优化 prompt 更可靠，尤其包含音效、对白和镜头顺序时。
- 下载结果会制造本地文件管理负担；画布流默认只沉淀画布，审片前再下载。
- 分镜参照流未实现时宁可阻断，也不要把主体参照流伪装成分镜参照流。
