# Providers Slot Notes

本目录只保留 `3-视频生成` 的 provider 槽位命名：

- `grok`
- `kling`
- `seedance`
- `sora`
- `veo`
- `vidu`

硬规则：

1. 这些目录默认不是本地 governed child skill。
2. 只有当某 provider 目录补齐 `SKILL.md + CONTEXT.md` 后，才可被视为本地可执行子技能。
3. 在此之前，`3-视频生成` 只把 provider 当作 handoff 目标名，不把它们当作“已实现能力”。
