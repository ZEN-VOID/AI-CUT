# CHANGELOG

## 2026-06-16

- 升级 `SKILL.md` 为 runtime-spine Skill 2.0 口径，补齐 Core Task、Business Requirement Analysis、Type Routing、Thinking-Action Node Map、Module Loading/Trigger、Convergence、Review Gate、Quantifiable Criteria、Attention、Checkpoint、Output 和 Learning 控制块。
- 新增 `test-prompts.json`，覆盖全量清单、增量合并、背景杂物 repair 和脚本归并拒绝场景。
- 同步 README 与 agent metadata，明确 `SKILL.md` runtime spine 仅为 legacy read-only reference，运行时节点真源在 `SKILL.md`。
- 强化 LLM-first 反脚本化门禁：脚本不得批量生成、批量插入、正则套句或映射投影 canonical 名称、归并理由、过滤判断或关键词描述。

## 2026-04-25

- 补齐 `SKILL.md` 的 `Visual Maps`，增加主流程、模式状态机和类型路由 Mermaid 拓扑，响应 Skill 2.0 工作车间的动态引用可视化要求。
- 补齐 `SKILL.md 的 Thinking-Action Node Map` 的工作流 Mermaid 与时序图，使节点、失败回路、review gate 和 canonical 落盘路径可视化。
- 初始化 `aigc-prop-list` Skill 2.0 包结构。
- 建立上游 `8-分组` 组底 YAML `道具` 字段到 `3-主体/道具/1-清单/道具清单.md` 的合同。
- 固定道具清单三列字段、别名归并、背景杂物过滤和脚本只做机械辅助的边界。
