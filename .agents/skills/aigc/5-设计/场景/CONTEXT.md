# CONTEXT.md

## Purpose & Loading Contract

- 本文件是 `aigc/5-设计/场景` 的经验层知识库。
- 调用 `.agents/skills/aigc/5-设计/场景/SKILL.md` 时，应自动预加载本文件。

## Context Health

- 当前文件只保存场景设计域的经验，不承载项目级场景正文、设计总稿或 provider 运行日志。
- 若经验影响整个 `5-设计` 阶段，应晋升到父级 `CONTEXT.md` 或 `SKILL.md`。

## Reusable Heuristics

- 场景清单先解决“有哪些空间和状态”，场景设计再解决视觉、材质、氛围与叙事功能。
