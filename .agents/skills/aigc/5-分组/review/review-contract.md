# Review Contract

本文件定义 `5-分组` 的质量门禁。

## Review Modes

| mode | trigger | action |
| --- | --- | --- |
| `entry_shot_review` | 每次阶段 PASS 前 | LLM 或人工检查每个分镜组在场景标题行之后是否有 `入场镜头：`，且它是否描述进入方式而非旧式静态定场：首组是否从本组第一个原始分镜可见状态进入，第二组及之后是否优先从上一组 `出场画面：` 尾钩进入，并结合上一组末帧、上一组尾帧、上一组最后一帧或连接件抵达首态校正；是否写清入口源、入场触发、镜头/观看位置、主体入画状态、动作相位、视线/距离/遮挡/朝向变化，以及必要的场景/环境入口信息；场景身份、环境身份、氛围、光影、色彩和声音底色是否只作为入场路径上下文，没有喧宾夺主变成“这是什么场景”的静态说明；是否有 `画面构图：`，并按固定顺序完整输出 `左侧：`、`中间：`、`右侧：`、`前景：`、`中景：`、`背景：` 六个构图分区，把全部可见或需要生成角色、群体、关键道具和场景锚点布置为带形容属性、状态属性、站位属性、空间方位关系、身体/视线朝向和动作相位变化的主体账本；每个分区是否展示具体可见主体、环境锚点、遮挡、光影材质或空场压力，并说明该分区如何引入、压住、牵引、遮挡、靠近、拉开或改变主体关系，而非“主体保持当前组正文中的站位”“关键动作锚点由本组正文中的……”或“固定环境锚点保持可读”等泛化占位句；是否没有在组头额外输出 `角色：` / `场景：` / `道具：` 清单；构图分区是否可回指 `4-摄影` 的画面、角色动作、镜头设计、分镜明细、对白主体、道具特写、上一组 `出场画面：` 或上一组尾帧，而非臆造位置；是否未退化为静态站位清单或“谁在做什么”的人物动作摘要；是否未新增无互动道具氛围镜头、道具互动事实、人物或剧情 |
| `exit_picture_review` | 每次阶段 PASS 前 | LLM 或人工检查每个分镜组在原正文之后、YAML 之前是否有 `出场画面：`；是否回指本组最后一个原始分镜、本组末帧、本组尾帧或本组最后一帧；是否写清可见主体、空间关系、动作余势、镜头/观看位置和下一组入场或连接件可承接锚点；是否没有新增人物、剧情、道具互动、未来动作、下一组首态或无证据空间出口；是否能被下一组 `入场镜头：` 和组间连接件具体承接 |
| `visual_tone_review` | 每次阶段 PASS 前 | LLM 或人工检查每组在入场镜头之后、north_star 风格行之前是否有画面属性自然语句，画面属性是否从组内镜头设计中提炼且与镜头设计一致，是否覆盖构图布局核心选择、构图方式关键子维度、光源效果、色彩基调、关键摄影技术参数、机位高度/前景层次/观众发现路径等主要空间感来源 |
| `mechanical_check` | 每次落盘前或修复后 | 运行 `scripts/validate_storyboard_groups.py`，检查分镜组标题、场景标题行、入场镜头、画面构图、六个构图分区完整性、固定顺序、非泛化占位内容、第二组起入场镜头上一组 `出场画面：` 或尾帧还原入场锚点、出场画面字段、必填标题、计入场景标题行/入场镜头/画面构图/构图分区/画面属性/出场画面后的正文字数、显式时长累计、YAML、组间连接件结构；warning 进入语义复核，不直接等于 PASS |
| `boundary_review` | 每次阶段 PASS 前 | LLM 或人工检查每组显式 `分镜N（约X秒）` 累计是否接近 15 秒、通常是否落在约 12-18 秒、且必须不超过 18 秒；atomic unit 与对白/画面承托必须完整，但不能作为超 18 秒放行理由；单个 atomic unit 超 18 秒时必须回退 `4-摄影` 修复；字数和对白数只作为辅助风险检查 |
| `bridge_review` | 每次阶段 PASS 前 | LLM 或人工检查组间首尾帧连接件是否能从第 N 组 `出场画面：` / 原尾帧抵达第 N+1 组 `入场镜头：` / 原首帧，是否以标题作为唯一连接件 ID，是否标题后先写场景标题行并在跨场景时使用 `场景标题A ➡️ 场景标题B`，是否在 `连接类型` 前写三项风格行，其中第 1 行是否根据连接件首尾场景和运动过程整理 300 字以内全局风格句，是否为 3-4 秒连接件，是否区分同场景/跨场景，是否把内部方法论选择转换为具体画面连接办法，是否避免复述起点/目标端点，是否以变化过程、主体运动、运镜设计和透视适应描述连接过程，是否以 `避免元素` 承载负面约束，是否非新增剧情且不在连接件块内重复 `出场画面：` |
| `faithfulness_review` | 每次阶段 PASS 前 | diff 或等价对照上游 `4-摄影`，确认正文字段、对白、原有分镜明细未被改写 |
| `statistics_evidence_review` | 每次阶段 PASS 前 | LLM 或人工抽查 YAML `角色`、`场景`、`道具` 是否能回指本组正文证据、入场镜头、构图分区主体信息和出场画面；道具项必须检查是否存在普通环境物过度列入、同物异名、状态差异、持有人差异、镜头差异或部件拆分造成的重复，并合并到稳定 canonical 道具名；YAML 只保留短名单统计，不承载形容属性、状态属性或站位叙述；高风险组和新增/异常统计项逐项复核 |

## Acceptance Checklist

| gate_id | check | pass criteria |
| --- | --- | --- |
| `GATE-GROUP-01` | 输入回指 | frontmatter 或报告记录 `source_cinematography_path`、`north_star_path` |
| `GATE-GROUP-01A` | 场景标题行 | 每个分镜组标题后先写当前场景标题行，未切换场景的新分镜组也重复同一场景标题；每个连接件标题后先写场景标题行，同场景重复同一标题，跨场景使用 `场景标题A ➡️ 场景标题B` |
| `GATE-GROUP-01B` | 入场镜头与构图分区 | 每个分镜组在场景标题行之后、画面属性和 north_star 风格行之前必须有 `入场镜头：`、`画面构图：`，并按固定顺序完整输出 `左侧：`、`中间：`、`右侧：`、`前景：`、`中景：`、`背景：` 六个构图分区；`入场镜头：` 必须说明入口源、入场触发、镜头/观看位置、主体入画状态和动态关系变化，首组从本组第一个原始分镜可见状态进入，第二组及之后优先从上一组 `出场画面：` 尾钩进入，并结合上一组末帧、上一组尾帧、上一组最后一帧或连接件抵达首态校正；场景身份、环境身份、氛围、光影、色彩和声音底色只作为入场路径必要上下文，不得写成旧式静态定场段落；构图分区必须把全部可见或需要生成角色、群体、关键道具和场景锚点布置到准确画面区域，并写出必要形容属性、状态属性、站坐蹲躺靠等姿态、前后左右内外远近等空间方位关系、身体/视线朝向和距离/遮挡/动作相位变化；每个分区必须展示具体可见主体、环境锚点、遮挡、光影材质或空场压力，并说明它如何引入、压住、牵引、遮挡、靠近、拉开或改变主体关系，不得空缺、乱序、使用泛化占位句或退化为静态站位清单；组头不得额外输出 `角色：` / `场景：` / `道具：` 清单；不得退化为人物动作摘要；不新增人物、剧情、道具互动或无互动道具氛围镜头 |
| `GATE-GROUP-01C` | 出场画面尾钩 | 每个分镜组在原正文之后、YAML 之前必须有 `出场画面：`；必须回指本组最后一个原始分镜、本组末帧、本组尾帧或本组最后一帧；必须写清可见主体、空间关系、动作余势、镜头/观看位置和下一组入场或连接件可承接锚点；不得新增人物、剧情、道具互动、未来动作、下一组首态或无证据空间出口 |
| `GATE-GROUP-02` | north_star 整理 | 每组组头含三行纯内容；第 1 行以固定前置词 `视频生成的画面风格，光影和氛围与场景参照图保持一致。需要生成现场物理互动音效、氛围感音效、环境声、自然现象声、动作声，不要生成任何字幕，不要生成背景音乐。` 开头，后接当前组全局风格整理句；整理句必须依据 `全局风格.全局风格提示词` 和当前组场景证据抽取匹配部分，保持自然单段且 300 字以内，不得完整照抄全局母稿或凭空新增当前组不存在的光源、色彩、天气、材质、动作；整理句必须保留作品级风格签名，至少覆盖作品身份锚点、当前组场景适配锚点和材质/摄影锚点，不能削弱成局部物件或普通光影描述；第 2、3 行分别直引 `类型元素.类型元素提示词`、`细分风格.画面风格`；不显示 `[全局风格]`、`[类型元素]`、`[画面风格]` 标题字段 |
| `GATE-GROUP-05` | ID 真实 | 每组 ID 为 `x-y-z`，匹配真实集、场、场内序号 |
| `GATE-GROUP-06` | 组内时长 | 每组以 `分镜N（约X秒）` 累计为主，优先接近约 15 秒，通常约 12-18 秒可接受；低于约 10 秒必须有回填复核或短场景例外理由；超过 18 秒为硬失败，必须拆分、重组或回退 `4-摄影` 修复，不允许完整 atomic unit 例外放行 |
| `GATE-GROUP-07` | 字数、对白与密度风险 | 字数和对白数只作为辅助风险检查；长对白约 4 句、短对白约 6 句是风险提示；高于 1680 字或低于 850 字的组必须进入语义复核，但不得因此切断同一画面或对应对白承托 |
| `GATE-GROUP-08` | atomic unit | 画面字段及其 `分镜明细/分镜N` 不跨组 |
| `GATE-GROUP-09` | 组间连接件 | 每对相邻分镜组之间都有 `## <上一个分镜组ID>~<下一个分镜组ID>`，连接件物理夹在上下两个分镜组中间，标题作为唯一连接件 ID，标题后先写场景标题行，同场景重复同一标题，跨场景使用 `场景标题A ➡️ 场景标题B`；随后在 `连接类型` 前写三项风格行，且第 1 行以固定全局风格前置词开头并接 300 字以内连接件风格整理句；字段包含连接类型、连接方法、时长、变化过程、主体运动、运镜设计、透视适应和避免元素；不得包含 `起点尾帧：` / `目标首帧：` / `分镜ID：` / `连接件提示：`，不得在连接件块内重复组内 `出场画面：` 字段；`连接方法` 必须是具体画面连接办法，不得只填抽象分类名；`避免元素` 不复述正向提示 |
| `GATE-GROUP-10` | 非新增剧情与非尾钩 | 连接件不新增关键事实、对白、人物或道具，不写成悬念尾钩、剧情解释或下一剧情预告 |
| `GATE-GROUP-11` | YAML 统计 | 每组含 `字数统计`、`时长估算`、`角色`、`场景`、`道具`；`字数统计` 必须计入分镜组标题后的场景标题行、入场镜头、画面构图、构图分区、画面属性和出场画面，但不得计入 YAML fenced block 本身；`时长估算` 只回指组内 `分镜N（约X秒）` 累计，其他统计项能回指本组正文证据、入场镜头、构图分区主体信息和出场画面；YAML 只写 canonical 短名单，不写站位段落或形容属性；`道具` 已完成同物识别、归一合并和去重，不把同一物品的状态、持有人、镜头景别、局部部件或普通环境描述重复列入；或在执行报告中记录已修复/排除的异常项 |
| `GATE-GROUP-12` | 画面属性 | 每组在入场镜头之后、north_star 风格行之前有画面属性自然语句；画面属性从组内镜头设计中提炼，覆盖构图布局核心选择、构图方式关键子维度、光源效果、色彩基调、关键摄影技术参数，以及必要的机位高度、前景层次、透视关系或观众发现路径；画面属性与组内镜头设计一致，不是参数清单或标签列表 |
| `GATE-GROUP-13` | 原文保真 | 划定正文同步原换行，未改写 `4-摄影` 字段、对白、原有分镜明细 |
| `GATE-GROUP-14` | 输出路径 | 写入 `projects/aigc/<项目名>/5-分组/第N集.md` 和 `执行报告.md` |

## Semantic Pass Gate

阶段 PASS 必须同时满足：

- `mechanical_check` 已运行且 error 为 0；warning 必须在语义 review 中处理。
- `boundary_review`、`entry_shot_review`、`exit_picture_review`、`bridge_review`、`faithfulness_review`、`statistics_evidence_review`、`visual_tone_review` 均记录 LLM 或人工结论。
- 语义结论只能为 `pass` 或 `pass_with_declared_exception`；任何 `fail` 都必须按 Failure Routing 返回源层修复。`>18s` 单组超时不得作为 `pass_with_declared_exception`。

## Failure Routing

| fail_code | symptom | rework target |
| --- | --- | --- |
| `FAIL-GROUP-02` | north_star 三项缺失；全局风格整理句缺失、超过 300 字、完整照抄全局母稿、未匹配当前组证据、凭空新增风格元素，作品级风格签名被削弱成局部物件或普通光影描述，或全局风格固定前置词缺失/未置于最前 | `references/north-star-projection-contract.md` |
| `FAIL-GROUP-04` | ID 不匹配或不连续 | `references/group-boundary-contract.md` |
| `FAIL-GROUP-05` | 组内显式时长累计明显偏离 15 秒且未复核、低于约 10 秒未回填、超过 18 秒、单个 atomic unit 超 18 秒但未回退 `4-摄影` 修复，或字数/对白风险失控 | `references/group-boundary-contract.md` |
| `FAIL-GROUP-06` | 同一画面句子、对应对白/画面承托或分镜明细被截断 | `references/group-boundary-contract.md` |
| `FAIL-GROUP-07` | 连接件缺失、字段不完整、场景标题行缺失或跨场景未用 `场景标题A ➡️ 场景标题B`、三项风格行缺失、连接件全局风格整理句缺失/超 300 字/与首尾场景不匹配或全局风格前置词错误、标题 ID 不是 `上一个分镜组ID~下一个分镜组ID`、块内重复输出 `分镜ID：`、连接方法缺失或只填抽象分类名、变化过程/主体运动/运镜设计/透视适应缺失、复述起点/目标端点、未夹在上下分镜组中间、无法从上一组出场画面/尾帧抵达下一组入场镜头/首帧、继续使用旧 `入场画面：`、在连接件块内重复 `出场画面：`、继续输出 `连接件提示：` 或新增剧情 | `references/bridge-shot-contract.md` |
| `FAIL-GROUP-08` | YAML 缺字段、统计错误，或道具未做同物识别与归一合并导致重复/过细 | `references/statistics-yaml-contract.md` |
| `FAIL-GROUP-09` | 改写上游正文 | `SKILL.md` Output Contract 与本文件 `faithfulness_review` |
| `FAIL-GROUP-10` | 画面属性缺失、与镜头设计不一致、退化为参数清单或标签列表，或遗漏组内关键机位高度、前景遮挡、透视拉伸、手持状态和发现路径导致空间感漂移 | `references/group-visual-tone-contract.md`、`steps/grouping-workflow.md#N4-VISUAL-TONE` |
| `FAIL-GROUP-11` | 入场镜头缺失、位置错误、缺少入口源、入场触发、镜头/观看位置、主体入画状态或动态关系变化，首组没有从本组第一个原始分镜可见状态进入，第二组及之后没有优先从上一组 `出场画面：` 尾钩进入并结合上一组末帧、上一组尾帧、上一组最后一帧或连接件抵达首态校正，入场镜头写回旧式静态场景说明，缺少 `画面构图：`，缺少 `左侧/中间/右侧/前景/中景/背景` 六字段任一字段，六字段顺序错误，分区内容空泛或使用泛化占位句，构图关系退化为静态站位清单，主体信息退化为 `角色：` / `场景：` / `道具：` 组头清单，角色/群体/关键道具缺少必要属性或站位，退化为人物动作摘要，未覆盖全部可见/需生成角色的站位姿态朝向和距离/遮挡/视线/动作相位变化，或新增无互动道具氛围镜头、人物、剧情、道具互动事实 | `steps/grouping-workflow.md#N5E-SUBJECT-EVIDENCE`、`references/group-entry-shot-contract.md`、`../../_shared/scene-shot-identity-contract.md`、`../../_shared/action-first-continuity-contract.md`、`steps/grouping-workflow.md#N5A-ENTRY-SHOT` |
| `FAIL-GROUP-12` | 出场画面缺失、位置错误、未回指本组最后原始分镜/末帧/尾帧、主体末态/空间关系/动作余势/镜头观看位置/承接锚点缺失，或新增未来动作、下一组首态、新剧情、新人物、道具互动、无证据空间出口 | `steps/grouping-workflow.md#N5B-EXIT-PICTURE`、`references/group-exit-shot-contract.md`、`../../_shared/scene-shot-identity-contract.md`、`../../_shared/action-first-continuity-contract.md` |

## Review Output

执行报告至少记录：

- 输入文件、north_star 文件与输出文件。
- 处理集号和场景数量。
- 分镜组数量与每场组号范围。
- 每组时长估算、字数、对白数、入场镜头入口源/触发/观看位置、画面构图、构图分区、动态关系变化依据、出场画面末帧来源/主体末态/承接锚点，以及 YAML 角色/场景/道具短名单概览。
- 组间连接件检查结果，包括相邻组 ID、场景标题行、三项风格行、连接类型、连接方法、变化过程、主体运动、运镜设计、透视适应、避免元素和 3-4 秒连接件语义结论。
- 机械校验结果，包括 error / warning 数量。
- `boundary_review`、`entry_shot_review`、`exit_picture_review`、`bridge_review`、`faithfulness_review`、`statistics_evidence_review`、`visual_tone_review` 的语义结论、抽查范围、时长偏离和例外理由。
- 需要返工的分镜组 ID 和失败码。
