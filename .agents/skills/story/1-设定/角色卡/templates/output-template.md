# Character Card Output Template

## Output Contract Alignment

| Output Contract field | Template alignment |
| --- | --- |
| Required output | `2-角色卡/**/*.json` and optional `角色关系图谱.md` |
| Output format | JSON payload aligned to `character-card.json`; graph as Markdown |
| Output path | `projects/story/<项目名>/1-设定/2-角色卡/` |
| Naming convention | ASCII safe id or project naming convention; graph filename fixed |
| Completion gate | parent writer success and graph/interface gates pass |

## Delivery Shape

- Character cards:
- Relationship graph:
- Upstream interfaces:
- Verification:
