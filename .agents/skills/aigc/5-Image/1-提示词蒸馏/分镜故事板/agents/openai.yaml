interface:
  display_name: "AIGC 5-Image 分镜故事板"
  short_description: "把分镜组蒸馏为多格 storyboard 图像请求 JSON"
  default_prompt: "Use $aigc-storyboard-sheet to read `projects/<项目名>/3-Detail/第N集.json`, extract each `分镜组` into `storyboard_group`, fill the shared image request template, and write the canonical `第N集.json` under `projects/<项目名>/5-Image/分镜故事板/`."
