# CONTEXT

> Created by `$aigc-init` as the neutral project context root. Later owning workflows may add relevant context files here.

项目：__PROJECT_TITLE__

本目录保存当前项目跨阶段共享的预设包、参考材料索引、风格约束、模型参数记录与非偏好型资料。项目级长期记忆、团队配置、初始化资料吸收摘要和阶段读取指南仍以项目根 `MEMORY.md` 为中枢。

## 边界

- 项目偏好、团队配置、资料吸收摘要、禁区与长期要求写入项目根 `MEMORY.md`。
- 大段原始资料、资料索引或模型参数侧车可放在本目录，但必须在 `MEMORY.md` 留下精简吸收摘要与指针。
- live route truth 仅在治理 workflow 需要时写入项目根 `STATE.json`。
- 技能经验、失败复盘和跨项目 heuristic 写入对应 skill 的 `CONTEXT.md`。
- 本目录不替代阶段 canonical 输出；阶段真源仍写入 `1-分集/` 到 `10-画布/` 的对应目录。
- 本目录不承载独立团队运行时，不生成或替代 `team.yaml`。

## 当前内容

- 暂无项目补充上下文
