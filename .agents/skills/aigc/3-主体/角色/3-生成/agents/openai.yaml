schema_version: 1
interface:
  display_name: "AIGC 角色图像生成"
  short_description: "从 角色/2-设计 设计文档调用 libTV，生成角色主图和同名 JSON prompt。"
  default_prompt: "使用 $aigc-design-character-generation，从 projects/aigc/<项目名>/3-主体/角色/2-设计/ 读取角色设计文档，为每个角色调用 libTV 生成到 3-主体/角色/3-生成/<主体ID>-<主体名称>-主图，并同时落同名 JSON prompt；多视图默认取消，不生成、不补齐、不验收。"
metadata:
  skill_id: "aigc-design-character-generation"
  governance_tier: "full"
  output_root: "projects/aigc/<项目名>/3-主体/角色/3-生成"
