# Subject Reference Default Type

固定上下文：

- C 路线以 `4-分组` 的完整分镜组正文和组底 YAML 为主体槽位真源。
- 主体参照只来自 `5-设计/*/3-生成` 中真实存在的角色、场景、道具图片。
- 有主体图片时先通过 `$libTV` 上传为 OSS URL，再把 URL 编号写在对应主体信息后。
- 无主体图时移除空参照槽位，允许走纯文本 LibTV 会话。
- 视频时长不固定 15 秒；默认读取当前组 `时长估算`，按 4-15 秒范围 clamp 后写入 `duration_hint`。
