# Changelog

## 2026-04-24

- Upgraded `aigc-detail` into a full Skill 2.0 package with canonical `steps/`, `review/`, `types/`, `knowledge-base/`, `templates/`, `README.md`, and `CHANGELOG.md`.
- Rewrote `SKILL.md` as a dynamic-reference entry while preserving the required `Business Requirement Analysis`, `Internal Capability Fusion`, `Topology`, `Mermaid`, `Thinking-Action Node`, `Academy Knowledge`, `One-Shot Output`, `Field Mapping`, and Root-Cause contracts.
- Added `steps/detail-thinking-action-workflow.md` as the canonical node and convergence owner, while keeping `references/思行网络.md` as a compatibility reference for existing validators and links.
- Added `review/review-contract.md`, `types/type-map.md`, `knowledge-base/detail-heuristics.md`, and `templates/output-template.md` to separate review gates, patch typing, reusable heuristics, and output rendering.
- Shortened `agents/openai.yaml` metadata so it satisfies the Skill 2.0 validator while keeping `$aigc-detail` as the explicit default prompt entry.
