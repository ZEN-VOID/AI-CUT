# CHANGELOG

## 2026-04-24

- 使用 `skill-工作车间` 将 `北冥神功` 原地升级为 Skill 2.0 包，补齐 canonical 目录、`README.md`、`agents/openai.yaml`、`templates/output-template.md` 与各分区 owner。
- 将原 `SKILL.md` 中的思行节点、reviewer 触发、类型策略、输出模板和长期经验分别拆入 `steps/`、`review/`、`types/`、`templates/`、`knowledge-base/`，主合同收束为输入、路由、门禁和输出合同。
- 新增 `references/skill-2.0-upgrade-migration-matrix.md`，记录旧段落到新 owner 的迁移去向与引用同步结果。
- 收束 `CONTEXT.md` 的 Case Log 倾向，保留 Type Map、Repair Playbook 与 Reusable Heuristics。

## 2026-04-23

- 初始化 `北冥神功` 技能包，建立 `skills-update` 自学习吸收主合同。
- 新增 `CONTEXT.md`，沉淀升级点落点裁决与 learning deposition 的经验基线。
- 新增 `references/upgrade-point-absorption-map.md`，定义升级点判型、同步范围和 reviewer 触发器。
- 同轮接入 `.codex/registry/skills.yaml` 与 `.codex/registry/routes.yaml`。
