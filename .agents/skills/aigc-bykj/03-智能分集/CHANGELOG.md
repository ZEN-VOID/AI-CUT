# CHANGELOG

## 2026-05-28

- 初始化 `03-智能分集` 整合型 skill。
- 将原 `aigc/1-分集` 的源锁定、集标判定、边界裁决、覆盖审计与报告能力压缩进单一 `SKILL.md`。
- 将输入对象从小说原文调整为 `output/[项目名]/02-剧本处理/` 的处理后剧本输出。
- 固定 canonical 输出目录为 `output/[项目名]/03-智能分集/`。
