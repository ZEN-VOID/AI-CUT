# Grouping Heuristics

本文件保存 `4-分组` 可复用经验。稳定且必须执行的规则应晋升到 `SKILL.md` 或 `references/`。

## Boundary Heuristics

- 先按场景建网格，再按组拆；不要先按 1980 字滚动切，再回头找场景。
- 候选组进入 1600 字后就要谨慎，因为 north_star 三项、站位位移和补位画面会吃掉剩余空间。
- 对白组的风险不是字数，而是视频生成注意力漂移；四句强对话已经常常需要拆。
- 一组最好只有一个主要注意力任务：环境建立、角色入场、规则显影、冲突升级、反应停顿、道具揭示等。

## Bridge Heuristics

- 出场画面应尽量落在静物、光线、焦点、声响余波或角色未完成动作上。
- 下一组入场画面复用上一出场画面后，再接原首帧，会比直接切原首帧更自然。
- 如果两个组之间没有共同视觉锚点，优先选择环境持续物：灯、门、窗、黑板、桌面、走廊线。
- 桥接画面不要替观众解释剧情，只让画面继续流动一两秒。

## Spatial Heuristics

- 空间锚点是内部参照系，不应作为独立字段输出；“讲台左侧”“车门外侧”“走廊尽头”等物理参照应直接进入 `站位和位移：`。
- `站位和位移：` 的最低有效信息是明确角色名、空间参照、方位；它必须归属到首次锁定或发生变化的具体 `分镜N:`，不能写成 `画面主体`、`主体`、`人物`、`角色`、`主角`、代词等含混主语，有移动时再补起点、终点或朝向，涉及多角色时补前后、左右、内外、近远或动作先后的顺位关系。
- 连续多个分镜的站位、位移、朝向和多角色顺位完全不变时，不要重复写 `站位和位移：`；保留首次锁定即可，把字数留给真正发生变化的空间调度。
- 同一组里如果角色频繁移动，优先描述会影响镜头连续性的第一位置和最后位置，不写成动作流水账。
- 每条位移动词都需要证据：当前分镜写了走、退、靠近、进出、转身，或上一组出入场补位给出了未完成动作，才允许写移动；否则优先写稳定站位、朝向或镜头变化。
- 镜头推拉摇移、切焦、凝视、沉默和心理压迫不是角色移动。若把这些写成“角色走向/退到/绕过”，后续视频阶段会产生错误空间调度。
- 组间连续性要看上一组出场画面、本组入场画面和本组第一条 `站位和位移：` 三者能否接上；只做到入场/出场文本相同还不够。

## Statistics Heuristics

- 角色统计宁可少而准，不要把被提及但未出现的人列入。
- 道具统计只列会影响后续设计或生成一致性的东西。
- 场景统计可以简化 slugline，但必须保留可回指的场所名。
